# Daily-fix run — 2026-06-27

Unattended scheduled run. Goal: find next 10 tasks, complete safe ones.

## Health baseline
- `tsc --noEmit` → clean
- `vitest run` → 136/136 pass
- No `TODO`/`FIXME`/`console.log` in `src`
Codebase is healthy. No defects breaking build/tests.

## Verified status of the design backlog (DESIGN_WEAKNESSES.md is partly stale vs code)

| ID | Weakness | Actual code state | Verdict |
|----|----------|-------------------|---------|
| W1 | Flat cards, no elevation | `Card`/`WhiteCard` primitive in use across dashboard; **one holdout** Compliance card had an invisible border | **FIXED this run** |
| W2 | Generic marketing hero / composite testimonial / fake logos | testimonial removed in `goLanding.tsx`; BUT `index.html` (Webflow home) still ships **27 fabricated company logos** (`/img/logos/trust-*`, `testimonial-*`, `case-*`) used 81× | **REMAINING** (Webflow) |
| W3 | Color-world whiplash | `App.tsx` depth-tone ground ("one descent") already landed (commit 1365324) | partial / subjective |
| W4 | Lemon overloaded | Design E lemon-rationing swept across 13 pages (commits 07f97c9→243d964) | DONE |
| W5 | Hero payoff numbers dim | gauge headline forced cream-bright (commit 3f329a4) | DONE |
| W6 | Sparse dashboard / `$0.0M` stat | dashboard is two-column (pipeline + compliance + alerts); stat reads `+$0.4M MTD`, not `$0.0M` | already addressed |
| W7 | Redundant "+ New deal" CTAs | both still present (sidebar `:646` + header `:743`); hero-skeleton repetition | REMAINING (subjective) |
| W8 | Login underline / hero scrim | login underline fixed (commit 8b5ed0f); hero scrim lives in `index.html` | partial |

## Completed this run
1. **W1 holdout** — `ComplianceDashboard.tsx:818`: Compliance card had `border: 1px solid swatch.midnight` on a `swatch.midnight` background (a literal no-op border). Replaced with the elevation language the rest of the app uses: `border: 1px solid rgba(238,239,211,0.16)` + `inset 0 1px 0 rgba(238,239,211,0.06)` top-highlight. Build + tests stay green.

## Next tasks — ranked, NOT auto-executed (need human visual review)
These were deliberately not bulk-applied in an unattended run: they are subjective redesigns or edits to the 2,525-line Webflow `index.html` that need rendered-page verification and taste judgment.

1. **W2 — fabricated logos/testimonials (trust + legal risk).** Remove or honestly label the 27 fake firm logos in `index.html` (`/img/logos/*`, 81 uses). `goLanding.tsx` already set the correct position: "no competitor logos — we are the lender." Align the Webflow home to it.
2. **W2 — bespoke hero.** Replace the stock "two men talking" hero with an abstract of the engine (gauge / flight-path / heatmap).
3. **W7 — dedupe "+ New deal".** Decide one primary; differentiate sidebar vs header.
4. **W7 — break the hero skeleton.** Left-copy·right-artifact·3-stat repeats across ~13 tools; extend the RateQuiz/ARM variants.
5. **W8 — hero text-over-photo scrim** in `index.html` (lower H1 lines drift over light wall).
6. **W3 — collapse background worlds** at the marketing↔app seam; carry shared motifs across.
7. **W1 sweep** — confirm no other primitive holdouts on remaining tool pages.
8. **Logo a11y** — the 81 logo `<img>` have `alt=""`; if kept, give meaningful alt; if decorative, keep but that conflicts with their "trust" purpose.
9. **W7 — long home H1** mid-phrase wrap in `index.html`.
10. **Lighthouse/perf pass** on the Webflow home (27 logo PNGs lazy-loaded; check LCP/CLS).

Recommendation: items 1, 2, 5 (W2/W8 honesty + hero) are the highest-leverage and should be done with a human in the loop on a preview deploy.
