# Greenstreet Finance — Ultimate Big-Picture Audit

**Date:** 2026-08-08 · **Method:** 14 independent audit lanes + 1 adversarial verifier + 1 completeness critic (17 agents, ~1.5M tokens, 382 tool calls, read-only — nothing was modified)
**Result:** **165 findings — 34 critical · 64 high · 52 medium · 15 low** · 1 claim refuted on verification · 107 strengths recorded

Lanes: memory systems · knowledge graph · docs sprawl · prior-audit reconciliation · repo hygiene · lending compliance · content truth · strategy · funnel/UX · engine model risk · deployment topology · security posture · SEO/GEO · brand & media spend.

---

## 0. Read this first — three corrections that change conclusions

Independent lanes contradicted each other. These are resolved:

1. **The research corpus is NOT lost.** The memory lane found `RESEARCH/`, `00_MOCs/`, `00_website/`, `ANALYSIS/`, `00_engine/`, `_obsidian_vault/` absent and called it a possible data-loss incident. The completeness critic then located all of it: **stranded inside seven orphaned agent worktrees** under `greenstreet_frontend/.claude/worktrees/` (7 dirs, 187–190 MB each, ~1.3 GB, all created 2026-08-01 18:20). It is recoverable — **but only until someone clears the agent cache.** Rescue it before any cleanup. This downgrades MEM-1/MEM-3 from *incident* to *urgent recovery*.

2. **The fake `555` phone is NOT live.** The prior-audit lane claimed a unit test now locks it in. Verification refuted this: `MarketingHome.test.ts:13-37` asserts `expect(publicMarketingMarkup).not.toContain(...)` — the number is in a **ban list**, and `MarketingHome.tsx:97-98` is a `CLAIM_REPLACEMENTS` pair that **strips** it. Decisive: `grep -c '555) 010-0000' dist/index.html` = **0**. Real residual risk is that the control is a fragile runtime string-replace, not that a fake number ships.

3. **The 903 MB SQLite DB and 2.4 GB datasets do not exist anywhere on this machine** — not in the tree, not in the recovered worktrees (`00_engine` there is 4.6 MB of markdown). A full-tree search for `*.db *.sqlite *.parquet *.csv` returned **zero files**. Memory describes them as core assets. Their status is genuinely unknown.

---

## 1. Verdict

Greenstreet Finance is a genuinely competent, test-covered DSCR calculation engine and a handsome marketing site wearing the costume of a licensed direct lender that does not legally exist. There is no published entity, no NMLS ID, no state license list, no physical address, no registered email domain, no working phone, and — verified this session — **no lead has ever reached a human, because the qualify form has a 100% failure rate**. The homepage tells brokers "we price it, structure it, and fund it — one team, no handoffs" beside 27 fabricated client logos (including *Vanguard Asset Management* and *Sequoia Capital Partners*), 5 invented testimonials, and an About page listing **eight fictional executives including a Head of Compliance and a Compliance Counsel** — while the site's own global footer admits it cannot identify its legal entity or licenses. The complete current version of the software exists in exactly one place: 146 uncommitted paths on one Windows laptop inside consumer OneDrive, with the production serverless entrypoint `api/index.js` **gitignored**, while the copy on GitHub announces "🎉 COMPREHENSIVE AUDIT — COMPLETE! Final Grade: A- (88/100)."

**The single biggest thing that will hurt you is not any one defect: this business has spent months manufacturing the evidence of things instead of the things — and the memory store and knowledge graph now read that manufactured evidence back as fact, so every AI session compounds the fiction instead of correcting it.**

---

## 2. Top 15 Things That Will Hurt You

Ranked by severity × likelihood × how soon. Drawn across all 14 lanes.

| # | What | Evidence | Fix |
|---|---|---|---|
| **1** | **The lead form rejects 100% of real submissions.** `QualifyModal` appends `createdAt` + `submittedAt`; the server schema is `.strict()` and lists neither → HTTP 400. Every CTA on every page funnels here. Zero leads have ever reached Firestore. Invisible because there is no analytics. | `QualifyModal.tsx:2117-2118`, `2130`; `src/routes/leads.ts:38-67` `.strict()`, `:148-152` → 400; the project's own `leads.test.ts:88-97` proves it | Delete lines 2117-2118. Server already stamps both. **Minutes.** |
| **2** | **Eight fabricated executives published as real staff**, including a Head of Compliance and a Compliance Counsel, used as blog bylines, under "The people behind the engine." Inventing a compliance officer for a lending business is a fabricated control function, not puffery. | `src/pages/AboutPage.tsx:6-15` (`TEAM` literal), `:292`, `:475`; Careers page simultaneously recruits for a Compliance Counsel | Delete the `TEAM` array and section. Re-byline to "Greenstreet Editorial". |
| **3** | **27 fabricated client logos + 5 invented named testimonials live**, four colliding with real litigious firms (Vanguard, Sequoia, Cornerstone, Lone Star). FTC 16 CFR §465.2 bans endorsements "by someone who does not exist"; Lanham §43(a) false association on top. | `greenstreet_frontend/index.html:979` (27 distinct `trust-NN-*.png`, 89 refs, all `alt=""`); `:~1264` "Maya Reynolds, Principal Broker, Nexus Financial"; identical in root `index.html` | Delete the trust wall and all testimonials from **both** copies; purge assets; preserve a copy for counsel. |
| **4** | **Every published contact channel is dead.** `greenstreetfinance.com` returns **NXDOMAIN** — the domain where legal and careers mail is published is not registered. `security.txt` (RFC 9116) formally directs vulnerability reports there. Privacy contact is a literal TODO. | Live DNS: NXDOMAIN for MX and A; `LegalPage.tsx:90,505`; `public/.well-known/security.txt:1`; `LegalPage.tsx:140` "pending owner verification" | Register the domain or move every address to `greenstreet.finance`. Today. |
| **5** | **You hold out as a direct in-house lender with zero required disclosures — and your own footer says you can't establish you're a lender.** Grep for NMLS / Equal Housing / ECOA = **0 matches** sitewide. | `BrokersPage.tsx:145-148` "we fund it in-house"; `SiteShell.tsx:405`; `LegalPage.tsx:62,78` | Pick one truthful posture sitewide today. |
| **6** | **The only complete copy of the business is un-backed on one laptop, and the public copy is the false one.** `api/index.js` — the entire serverless backend — is **gitignored**. Local `main` is 12 ahead / 47 behind origin, 146 dirty paths. 2.2 GB `.git` inside OneDrive sync. | `git check-ignore -v api/index.js` → `.gitignore:25:api/`; `git rev-list --left-right --count main...origin/main` → `12 47` | Fix `.gitignore`, commit everything, push to a dated branch. **Minutes to start.** |
| **7** | **Fabricated compliance attestations shown to brokers being asked for their own NMLS number** — four hardcoded green pills including an SEC 17a-4 WORM claim that doesn't apply to mortgage at all, and "State rules current: Synced" falsified by your own data. | `ComplianceDashboard.tsx:894-898` literal `as const`, reads no state; `:1684` collects broker NMLS | Delete the panel this week. |
| **8** | **Your own fail-closed release gate was bypassed, not satisfied.** 14 tools carry hold records with unmet evidence; 7 were quietly unwired and shipped — including the Tax Engine, held pending "independent tax-professional review." | `toolReliabilityHolds.ts` (14 records); `App.tsx:235`; `DecisionSupportPage.tsx:110` still `state:"TX"` | Re-attach holds for all 14; CI check on orphaned hold keys. |
| **9** | **Memory and the knowledge graph feed wrong facts into every session.** Memory canonizes a legally wrong PA Act 6 threshold as "verified"; a blanket "audits resolved" all-clear suppresses three newer audits; the graph indexes a stale **duplicate** repo where `firpta.ts` and `covenantCheck.ts` don't exist — under a mandatory-read CLAUDE.md rule. | `project_audits_resolved.md` `PA_PPP_THRESHOLD_2026=329_411` vs `statePppLaws.ts:36` `319_777`; `graph.json` — **0 nodes** from `greenstreet_frontend` | Delete the constant list and the graphify block from `CLAUDE.md` today. |
| **10** | **The homepage is a wall of broken images and a dead hero video.** `dist/index.html` references 55 `/img/...` paths + 2 videos; `find dist -name "*.png"` returns **zero**; `dist/video` doesn't exist. The OG share image 404s sitewide — on a LinkedIn-only go-to-market. | `dist/index.html`; `public/` has no `img/` or `video/`; `git ls-files public/img public/video` = 0 | Move real assets in and commit; delete the fabricated ones rather than restoring them. |
| **11** | **~3.1 GB of loan-level data is permanently in git history** — the 903 MB engine DB, HMDA CA/FL loan-level CSVs, FHLMC/FNMA PUDB, FEMA NFIP claims, and a 423 MB `everything_session_complete.zip` nobody has enumerated for credentials. | `git count-objects -vH` → 1.98 GiB pack; blob list | `git filter-repo --strip-blobs-bigger-than 5M` before anyone else gets repo access. |
| **12** | **Client-only SPA serving 54 sitemap URLs from one identical HTML shell.** No SSR, no prerender. Bing (which feeds ChatGPT), and every social/AI link scraper, sees the homepage for all 54 URLs — including the 33-post blog corpus the content strategy rests on. | `server.ts:28-30` `appType:"spa"`; `vercel.json` rewrites `/(.*)` → `/index.html`; no SSG plugin | Prerender the 54 routes at build time. |
| **13** | **Per-field provenance is meticulously maintained and then entirely discarded at the UI** — including data explicitly stamped "do not present as fact." The operator provably knew which numbers were unverified and shipped a UI that cannot honor its own warning. | `lenders.ts:131` `dp(4_000_000,'UNVERIFIED','…do not present as fact')`, 238 `source:` fields; `LenderIntelPage.tsx` — grep for confidence/sourceType/asOfDate = **0 matches** | Make the type refuse to unwrap a datapoint without emitting its provenance. |
| **14** | **CI has never run and cannot run** — `.github/` is untracked *and* is in the wrong directory (GitHub Actions only reads repo root; root is the parent). Meanwhile `DEPLOY.md` asserts CI guards the exact failure that is now unguarded. | `git status` → `?? greenstreet_frontend/.github/`; workflow at `greenstreet_frontend/.github/workflows/ci.yml` | Move to repo root with `working-directory`, commit, confirm a green run. |
| **15** | **Anonymous auth + no App Check + no deployed rules path.** Firestore rules are well written but **nothing deploys them** (`grep firestore:rules` across CI/scripts = 0), and two different Firebase projects plus a named AI-Studio database are in play. A rules file in a repo is documentation, not a control. | `grep -rn "firestore:rules\|firebase deploy" .github/ package.json scripts/` = 0; `.firebaserc` vs `_to_delete/firebase-applet-config.json` — different `projectId` | Confirm which project/database holds `deals`/`leads`; add a deploy step. |

**Also urgent, non-obvious:** the sole human path in the entire business is one third party's personal HubSpot calendar (`meetings-na2.hubspot.com/mark-michael/...`, 8 occurrences) — a name that appears in no memory file, no doc, and nowhere in About.

---

## 3. The Meta Problem

**You keep producing the artifact that represents a fact instead of the fact — then reading your own artifact back as evidence.**

| The thing | The artifact produced instead |
|---|---|
| Customers | 27 logos + 5 named testimonials |
| A team | 8 fabricated executives incl. a Head of Compliance |
| Compliance | 4 hardcoded green status pills |
| A release gate | 14 hold records — 7 quietly unwired, defects intact |
| Remediation | "~34/42 closed, no regressions" — the 42 never listed |
| Quality | three grades published the same day: 72/100, 82→88, A- 88 |
| A safety net | a CI workflow never committed, in the wrong directory |
| A knowledge system | a 4% projection of the wrong repo, corpus inflated 14.6× |
| Institutional memory | "verified done" claims that were false when written |
| A phone line | a reserved-fiction number, stripped at render by a string replace |
| A licensed lender | a footer admitting there isn't one |
| A revenue path | a form that 400s, into a table nobody reads |

Individually these read as sloppiness. Together they are a **closed assertion loop**: assertions are cheap, nothing falsifies them, and they are now written into the memory store and knowledge graph that brief the *next* session. `project_home_fabricated_logos.md` is the clearest artifact: a session found the FTC exposure, the owner said "leave it for now," and memory converted that into a standing instruction to **actively preserve** fabricated endorsements on a lending homepage. Forty days later they are still live. **The memory system is now defending the single largest legal liability in the business.**

The tell is what has **never** happened across ~33 audit documents, 22 memory files and a 471-line graph report: not one end-to-end contact with reality. No CI run has ever executed. No lead has ever arrived. No tax professional has reviewed the tax engine. No entity formed. No license obtained. No phone answered. Every "done" is a document about being done.

**One rule breaks the loop: nothing may be marked complete on the basis of a document. Completion requires a test that fails when the claim is false, or an observation of the real world.**

---

## 4. Memory & Knowledge Systems — the verdict you asked for

**Both are net-negative right now. The memory store is poisoning sessions; graphify is actively misleading them under a mandatory-read rule.**

### 4.1 Memory store — poisoning, with a concrete legal blast radius

| Proof | Consequence |
|---|---|
| `project_audits_resolved.md` records `PA_PPP_THRESHOLD_2026 = 329_411` as audited and verified. Code has `319_777` (`statePppLaws.ts:36`) with a named regression test (`advisory.test.ts:350`). | Memory wins in a fresh session. An agent "restoring consistency" reverts a **Pennsylvania Act 6 statutory threshold** that determines whether a prepayment penalty is lawful. |
| Same file: every 2026-06 item "fixed or a false alarm," listing API keys, auth, rate limiting and Firestore rules as verified-clear. Its cited archive `docs/audits-archive/` **does not exist**. Three later audits (07-23, 07-25, 07-27) appear nowhere in memory. | A blanket, dated, non-expiring all-clear on the security posture of a lending platform — that argues against re-checking. |
| `project_home_fabricated_logos.md`: "It's a real credibility/compliance issue" + "Do NOT remove or relabel these unprompted." | **This memory is why the #3 finding above is still live 40 days after it was found.** |
| 8 of 21 memories describe `RESEARCH/`, `00_MOCs/`, `00_website/`, `ANALYSIS/`, `00_engine/`, `_obsidian_vault/` — none at their documented paths. `reference_website_copy_source.md` names `00_website/FRONTEND_HUB.md` as the source of truth for every rate, program and eligibility claim on the site. | Your public copy has **no retrievable source document** — the first thing a regulator or capital partner asks for. (The tree itself is recoverable from the worktrees; the *pointers* are all wrong.) |
| Newest memory (2026-07-02) claims "Zero old hexes remain," citing a commit — false at `StateLawsPage.tsx:32` (`#f97316`, `#ff6b6b`) and 3× in `index.html`. | Freshness and commit citations do not make a memory true. Discount every "verified" claim accordingly. |
| Three incompatible business models recorded, no supersession marker. Store last updated 2026-07-02; 5 weeks and 3 audit cycles unrecorded. No per-claim dates. | Which positioning a session builds toward depends on read order. |
| A "do not delete — 387 SHA-verified unique files" guard whose guarded tree vanished from its documented path and memory never noticed. | The guard failed silently. (Recovered — see §0.) |

**Net:** the index layer (`MEMORY.md`) is genuinely good and the entry format is auditable — that is the only reason this audit could falsify anything. The **content layer** is majority-stale, self-contradictory in at least three places, and in two cases is the direct proximate cause of a live legal and factual error.

### 4.2 Graphify — actively misleading, and the fix is hours away

`C:/Users/serge/CLAUDE.md` routes every architecture question through `graphify-out/GRAPH_REPORT.md` first. That report is built from a graph in which **not a single node comes from `greenstreet_frontend`** — the shipped codebase. Sixty engine modules are invisible: `firpta.ts`, `covenantCheck.ts`, `rentIntegrity.ts`, `creditMemo.ts`, `secondLienDscr.ts`, `dataVintage.ts`, `failClosed.test.ts`.

**The concrete wrong answer an agent gives today:** asked *"do we handle FIRPTA withholding, and what covenant-check coverage do we have?"*, an agent obeying the rule reads the report, finds no `firpta` or `covenantCheck` symbol among 367 nodes and 75 unnamed communities, and answers **"this codebase has no FIRPTA handling and no covenant checks."** Both files and both test suites exist. For a non-QM lender courting brokers and warehouse lines, confidently denying foreign-investor withholding logic is a compliance-grade error — and it is the **default** output of the current configuration. The mirror error is equally available: the graph confirms `wf.tsx` exists, which you deleted.

Compounding: **477 of 478 edges are intra-file** (`GRAPH_REPORT.md:102` says so out loud) so the community structure is mathematically incapable of clustering by concern; the corpus size justifying the system is **inflated 14.6×** (2,929,511 claimed vs 201,069 actual words); the `wiki/index.md` the rule prefers **has never existed**; and the rebuild command calls `python3`, which **is not installed** — which is exactly why nothing has rebuilt since 2026-06-24.

**The good news:** the graph you want already exists on disk. `graphify-out/cache/` holds **8,043 nodes, 13,012 edges, 608 files, 3,581 import edges, 127 `greenstreet_frontend` files** — 0 parse failures. `graph.json` kept 367 / 478 / 76 / **1**. This is a **publish-step misconfiguration, not a rebuild.** Recovery is hours.

### 4.3 Recommendation

Until both are repaired: **delete the graphify block from `CLAUDE.md` today**, and prepend one line to `MEMORY.md` stating that all memories predate 2026-07-02 and must be re-verified against source before being acted on. A session with no memory is meaningfully safer than one briefed on a wrong Pennsylvania statutory threshold and told fabricated testimonials must not be removed.

---

## 5. What's Actually Good

Not token. These are real and worth protecting.

1. **The engine math is real and test-locked.** PA Act 6 corrected to `319_777` **and** pinned with a named regression test. The flood-insurance unit bug that inflated the gating DSCR was fixed with an explanatory block at `engine.ts:461-495`: *"floodInsurance is already MONTHLY — do NOT divide by 12."* Fix → explain → test-lock is exactly right for a credit engine.
2. **The lead-loss defect was fixed architecturally, not patched** — client writes denied outright (`firestore.rules:90-91`), intake moved to a server route with the admin SDK. `QualifyModal` fails closed with an honest error rather than faking success.
3. **`/api/narrate` was closed on three axes:** unconditional `requireAuth`, a rate limiter at the mount point, and a production refusal — *"Refusing to send borrower financial data to an undeclared LLM endpoint."* The hardcoded z.ai proxy was genuinely removed. **Where the codebase and the docs disagree, the codebase is often the more careful of the two.**
4. **`StateLawsPage` no longer hand-codes legal conclusions** — imports `PPP_STATE_LAWS` and treats a missing entry as `NOT_RESEARCHED` rather than defaulting green. Fail-open fabricated legal guidance was a real HIGH and is properly fixed.
5. **Security headers are above average for a small fintech:** real CSP with `frame-ancestors 'none'`, HSTS `max-age=63072000; includeSubDomains; preload`, Permissions-Policy, nosniff, `X-Frame-Options: DENY`.
6. **No secrets are committed.** All 19,873 tracked files searched — only `.env.example` templates, with an explicit "NEVER commit .env" warning.
7. **`ToolReliabilityHold` is an unusually good idea** — a fail-closed per-tool gate naming exactly what evidence is required. The finding is that it was bypassed, not that it was wrong. The machinery exists; restoring this control is cheap.
8. **The 2026-07-23 nine-lane audit is genuinely honest**, leading with *"a genuinely good deterministic DSCR calculator wearing the marketing costume of an institutional AI lending platform you have not built."* Its prior-finding-reconciliation lane is a control most small companies never build.
9. **`REGRESSION_SWEEP`'s "Owner-supplied blockers still required — these are not safe to invent in code"** is the most honest artifact in the corpus. It refuses to fabricate entity, licensing and data-source facts. Preserve that discipline.
10. **`project_engine_build_constitution.md` refuses to fabricate** the missing golden tests and canary inputs: *"Gaps become surfaced 'needs verification,' never a training-data guess."* Refusing to invent numbers for a credit engine is the single most important behavior in the whole store.
11. **Test infrastructure is real:** vitest with separate node/dom projects, jsdom + testing-library, 44 targeted suites including `failClosed.test.ts`, `dataVintage.test.ts`, `auth.test.ts`, `rateLimitStore.test.ts`.
12. **Commit hygiene is degrading, not absent** — half the last 20 commits are properly conventional with real PR merges. The practice exists and can be restored rather than invented.
13. **The honest global footer disclosure is the legally correct interim posture.** Keep it — and make the rest of the site stop contradicting it.
14. **No git corruption yet** despite OneDrive: no conflict-copy files, HEAD resolves cleanly, `garbage: 0`. There is still time to move.

---

## 6. Three Horizons

### THIS WEEK — stop the bleeding

1. **Rescue the research corpus from the seven worktrees** before anything clears the agent cache. Copy `.claude/worktrees/*/` content out to a real location, verify, then delete the worktrees. *(§0.1, PRI-6)*
2. **Fix the lead form.** Delete `QualifyModal.tsx:2117-2118`, then submit the real form on production and confirm the document lands and a named human is alerted. Until this is done there is no business. *(FUN-1, FUN-2)*
3. **Delete the fabricated team** (`AboutPage.tsx:6-15`) and re-byline every blog post.
4. **Delete the trust wall and all testimonials from BOTH `index.html` copies**; purge `/img/logos/trust-*`. Preserve a copy for counsel first.
5. **Delete the hardcoded compliance panel** (`ComplianceDashboard.tsx:894-898`). Nothing that reads no state may show a green compliance badge to a licensed broker.
6. **Register `greenstreetfinance.com` or move every address to `greenstreet.finance`** — including `security.txt`.
7. **Strike every in-house-lender claim** (`index.html:1015-1031`, `BrokersPage.tsx:148`, `:272`) and the rate guarantee. Then grep sitewide for "in-house", "we fund", "direct lender", "no middleman" and treat every hit as a blocker.
8. **Re-attach `ToolReliabilityHoldPage` for all 14 held tools.** The Tax Engine must not be live while its own record demands independent tax-professional review.
9. **Fix `.gitignore` (`api/`, `*.cjs`) and commit everything** — `api/index.js`, the 146 dirty paths, `.github/`, `DEPLOY.md`, and all untracked audits. Push to a dated branch.
10. **Correct memory today:** delete the `PA_PPP_THRESHOLD_2026` claim and the blanket all-clear from `project_audits_resolved.md`; add an expiry + launch trigger to `project_home_fabricated_logos.md`.
11. **Delete the graphify block from `C:/Users/serge/CLAUDE.md`.** A wrong map under a mandatory-read rule is worse than no map.
12. **Delete section 2 of `MANUAL_ACTIONS_REQUIRED.md`** — a copy-pasteable destructive force-push instruction for a credential leak that never happened (its own check, `git log --all --full-history -- .env`, returns empty).

### THIS MONTH

13. **One authoritative site.** Destroy or archive the root `index.html` / `vercel.json` / `firebase.json` decoy — its config ships a financial site with **no CSP and no HSTS**. Add a CI check that fails the build if the footer disclosure string is missing from rendered output.
14. **Fix the assets.** Ship real `/img` and `/video`, or delete every reference. Then fix the OG image — the LinkedIn-only GTM is currently posting broken previews.
15. **Get out of the version-control danger zone.** Reconcile `main` ↔ `origin/main` (12/47) deliberately. Move the repo off OneDrive to `C:\dev\`. Tag deploys so you can answer "which commit produced this quote."
16. **Purge duplication.** `git rm -r --cached .claude/worktrees` (15,011 files, 75.5% of the repo) *after* step 1. Scan `_claude_sync.tar.gz` (150 MB, ships `.claude/` agent config) for credentials and rotate before deleting.
17. **Prerender the 54 routes** so each ships its own title, canonical, OG tags and JSON-LD. Fix the structured data pointing at `greenstreet.com` — a domain you do not own.
18. **Collapse the document pile.** Archive the 15 celebration markdowns with SUPERSEDED banners; keep `README.md` + one `STATUS.md` + one deploy doc; ban FINAL/COMPLETE/ULTIMATE in filenames.
19. **Build a persistent findings ledger with stable IDs.** A finding may only leave with a FIXED ruling citing current source, preferably a test name.
20. **Repair or retire graphify.** Diff `cache/` against `graph.json` to find the filter dropping 532 of 608 files and 3,580 of 3,581 import edges; re-emit from cache. Fix `python3` → `python` at source.
21. **Reconcile the memory store in one pass.** Stamp last-verified dates; retire the dead-path Reference tier; add the three July audits; fix the DcNav/SiteNav contradiction; move business terms into version-controlled docs.
22. **Publish a working privacy contact and rights process, or take the intake form down.** The policy currently states in its own text that it has neither, on a page collecting GLBA nonpublic personal information.
23. **Purge the 3.1 GB from git history** with `git filter-repo` before granting anyone repo access; enumerate `everything_session_complete.zip` (423 MB) for credentials and dataset licensing.
24. **Resolve the missing 903 MB database and 2.4 GB datasets** — moved, deleted, or never existed at scale. Write the answer down with a date.

### BEFORE THE FIRST REAL BORROWER

25. **Establish the legal facts or permanently drop the lender posture.** Entity name, NMLS company ID, per-state license list, physical address, Equal Housing statement — published in the global footer and on LegalPage. Or a documented sitewide posture as a software/analytics provider with every lender claim removed. **This gates everything else.**
26. **Counsel decision on Reg B.** Does the qualify flow constitute an "application"? If yes: adverse-action notices, ECOA agency notice, 12-month retention, before any further traffic. If no: sever the decision from identity capture.
27. **Satisfy every `whatIsNeeded` item** on all 14 hold records with dated sign-off — independent tax-professional review first. Delete a hold only in the same commit that lands its evidence.
28. **Fix the LLM advisor posture.** `narrate.ts:104` casts the model as *"a DSCR lending advisor speaking directly to the real estate investor"* and instructs it not to disclose it is AI — precisely the role `LegalPage` §1 disclaims.
29. **Make the revenue path survivable:** two independent lead delivery paths, a daily zero-leads alarm, a named human owner, a verified end-to-end test on record. Replace the third party's personal HubSpot calendar with a company-owned booking path.
30. **Decide what the tools are.** Marketing calculators (labeled non-authoritative) or underwriting instruments (server-computed and logged). Today they are labeled the former and positioned as the latter, with **0 `fetch()` calls in `src/pages`**.
31. **Green CI + branch protection + tagged releases**, so you can answer "which commit produced this borrower's quote" in a dispute or an exam.
32. **Real phone, real address, real E&O, verified billing** — and a permanent non-code audit lane covering entity, licensing, insurance, billing and a named owner per operating function. **That lane has never been run, and it is where the risk actually lives.**

---

## 7. Appendix A — Full findings, knowledge & operations lanes (64)

### 3.1 Lending regulatory & legal exposure (9 findings — 3 critical, 6 high)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| LEG-1 | critical | verified | 27 fabricated client logos + 5 invented named testimonials live in both site copies; 2 are real trademarks; "composite" label likely also false | `greenstreet_frontend/index.html:979`, `:~1264`; root `index.html` identical (81 trust- refs, 5 "Composite, based on real") | Delete both; purge assets; preserve for counsel | hours |
| LEG-2 | critical | verified | Hardcoded green compliance attestations incl. inapplicable SEC 17a-4 WORM, shown to brokers asked for their NMLS number | `ComplianceDashboard.tsx:894-898`, `:1679`, `:1684` | Remove panel; any badge must compute from real state | hours |
| LEG-3 | critical | verified | Marketing holds out as in-house direct lender; LegalPage admits role cannot be established; zero NMLS/license/EHO/address site-wide | `index.html:1015-1031`; `LegalPage.tsx:62`, `:78`; grep NMLS/EHO/ECOA = 0 | Pick one posture and make the whole site say it | days |
| LEG-4 | high | verified | "The rate you see up front is the rate at the table" is a rate guarantee contradicted by four of your own disclaimers | `index.html:~1031` vs `ComplianceDashboard.tsx:291,316`, `index.html:1199`, `LegalPage.tsx:167` | Delete the clause; attach "preliminary estimate" to every tier incl. the 6.9% badge | minutes |
| LEG-5 | high | verified | "50-state rules, always current… updated every month" false; 13 states 7 mo stale, 6 UNVERIFIED, engine says annual cadence | `index.html:~1027` vs `statePppLaws.ts:26-33`, `:202` | Match copy to cadence; surface vintage + UNVERIFIED count | days |
| LEG-6 | high | likely | Qualify engine issues automated declines with Reg B-shaped principal reasons alongside identity capture, with no adverse-action notice, ECOA agency notice, or 12-month retention. Reg B covers business credit (12 CFR 1002.9(a)(3)) | `qualify.ts:18`, `:194-204` ("Credit below the 660 program floor"), `:301-302`; `QualifyModal.tsx:61-64`, `:2100-2114`; no `adverse` logic anywhere | Counsel decision; cheapest safe path is to sever decision from identity capture | weeks |
| LEG-7 | high | verified | Published privacy policy states in its own text that it has no privacy contact channel, no rights process, and does not identify downstream recipients | `LegalPage.tsx:117`, `:131`, `:137`, `:140`; TERMS_DOC `:187`,`:190` | Stand up a real channel + rights list, or disable the intake until then | days |
| LEG-8 | high | verified | Two divergent site copies with materially different legal disclosure; the root copy lacks the protective footer entirely | `SiteShell.tsx:403-405` vs root `index.html`: "does not currently publish" = **0** occurrences, yet 81 trust- refs and 2 in-house claims | One authoritative source; CI check that fails if the disclosure string is absent | hours |
| LEG-9 | high | verified | 52 rate literals across 10 pages incl. rate+LTV+FICO combinations; `ComplianceNote` used on 1 of 38 pages; a bank-statement program is advertised, which weakens the business-purpose defense (Reg Z §1026.24 triggering terms) | BlogPage 23, RateQuizPage 6, FAQPage 5, TaxEnginePage 5, ARMPage 4…; `index.html:1199` "6.25% - 6.75% / 740+ FICO, up to 75% LTV" | Attach ComplianceNote to all rate surfaces; confirm no owner-occupied product is offered | hours |

### 3.2 Prior-audit reconciliation / meta-audit (11 findings — 2 critical, 5 high, 3 medium, 1 low)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| PRI-1 | critical | verified | Release gate silently removed for 7 tools whose named defects are still present | `toolReliabilityHolds.ts` (14 records); `App.tsx:235`, holds rendered at :460,478,488,490,544,546,548; `DecisionSupportPage.tsx:110` `state: "TX"` | Re-hold all 14; CI check on orphaned hold keys | hours |
| PRI-2 | critical | verified | Memory canonizes a legally wrong PA Act 6 threshold as "verified done" — and memory wins in a fresh session | `project_audits_resolved.md` `PA_PPP_THRESHOLD_2026=329_411` vs `statePppLaws.ts:36` `319_777` + `advisory.test.ts:348-351` | Delete constants from memory; constants live in source with tests | minutes |
| PRI-3 | high | verified | "~34 of ~42 prior findings fixed… no regressions found" is unenumerated and false — the next two audits found large new Critical/High sets 4 days later | `ULTIMATE_AUDIT_2026-07-23.md:16`, `:128`; no list of the 42 exists | Ban unenumerated pass rates; require finding-ID → ruling → file:line | days |
| PRI-4 | high | verified | UDAAP in-house-funding claim survives on BrokersPage — the sweep sanitized only the page the auditor cited | `BrokersPage.tsx:148` "no middleman… then we fund it in-house", `:272`; `dscrPrograms.ts:4` "partner, Cake (caketpo.com…)"; `dataVintage.ts:153` | Stop fixing by citation; forbidden-phrase grep in CI | hours |
| PRI-5 | high | verified | Fake 555 phone reduced 14→3 but the survivor moved to the homepage CTA **and acquired a unit test defending it** | `MarketingHome.tsx:97`, `home-markup.html:951`, `MarketingHome.test.ts:22` | Delete the assertion first, then the number; invert the test to ban reserved exchanges | minutes |
| PRI-6 | high | verified | 1.3 GB / 7 orphaned agent worktrees inside the OneDrive business folder — invisible to all three prior audits because they're gitignored | `du -sh greenstreet_frontend/.claude/worktrees` = 1.3G; `git worktree list` shows 3 unrelated paths; `serverApp.ts` exists **only** inside them | Delete after checking for unmerged work; move repo off OneDrive | minutes |
| PRI-7 | high | verified | Systemic blind spot: three audits graded the repository, none graded the business — no verification of entity, licensing, billing, or that any lead arrives | `RELEASE_AUDIT §3` owner test never closed; `leads.ts:82` → `firebaseAdmin.ts` throws without env var; no reader of the collection | Add a non-code audit lane: entity, licensing, insurance, billing, named owner per function | days |
| PRI-8 | medium | verified | Licensing gap "closed" by publicly confessing it rather than curing it — and the confession sits in the global shell contradicting the lender claims | `SiteShell.tsx:405`; `LegalPage.tsx:78` | Track as an owner blocker with a date, not a closed finding | weeks |
| PRI-9 | medium | verified | "Financial math is not server-authoritative" was dropped from the finding set, not resolved — audit-set decay | `grep -c "fetch(" src/pages` = **0**; `DSCRCalculatorPage.tsx:6-12` imports the engine directly; absent from both later audits | Persistent findings ledger with stable IDs; decide calculator vs underwriting instrument | days |
| PRI-10 | medium | verified | Prior audits cite ≥3 artifacts that don't exist: `docs/audits-archive/`, `serverApp.ts`, `.firebaserc`; test counts disagree (135 vs 156) | `ls docs/` → only DEPLOYMENT_GUIDE.md, RELEASE_AUDIT, audit-2026-07-23/, audit-2026-07-27/ | Validate every path citation; cite test names + SHAs, not line numbers | hours |
| PRI-11 | low | verified | The prescribed fix for documentation rot was more documentation — 17 root markdowns, 4 rival deploy docs, no archive created | ULTIMATE_AUDIT P1 item 13 unactioned; 2 new audit dirs added since | Archive all but README.md + one STATUS.md | hours |

### 3.3 Repo & operational hygiene (12 findings — 4 critical, 4 high, 3 medium, 1 low)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| REPO-1 | critical | verified | Production backend entrypoint `api/index.js` is gitignored — exists only on this laptop, and the remote is 47 commits divergent | `git check-ignore -v` → `.gitignore:25:api/`; `git ls-files api/` → only `_app.cjs`; `vercel.json` routes `/api`, `/api/(.*)`, `/health` to it | Narrow the ignore to `api/_app.cjs`; `git add -f api/index.js` | minutes |
| REPO-2 | critical | verified | 75.5% of the tracked repo is 7 duplicated agent worktree copies — nobody can establish which code ships | `git ls-files` = 19,873; worktrees = 15,011 across 7 dirs; `.claude/` ignored by neither .gitignore | `git rm -r --cached .claude/worktrees`; ignore `.claude/` | hours |
| REPO-3 | critical | verified | ~3.1 GB of loan datasets permanently in git history incl. 903 MB engine DB, HMDA loan-level, FEMA NFIP, and a 423 MB unaudited zip | `git count-objects -vH` → 1.98 GiB pack; blob list | `git filter-repo`; move to object storage; enumerate the zip | days |
| REPO-4 | critical | verified | Split-brain: 12 unpushed / 47 unpulled commits, 146 dirty paths, 5,685 lines of untracked TS incl. `failClosed.ts`, `rateLimitStore.ts`, `engine/brain/` | `git rev-list --left-right --count main...origin/main` → `12 47`; `git status --porcelain \| wc -l` = 146 | Stop feature work; commit + push today; reconcile deliberately | hours |
| REPO-5 | high | verified | 291 MB of untracked-and-unignored archives in `_to_delete/`; one `git add -A` welds them into history. `_claude_sync.tar.gz` packages `.claude/` agent config (usual home for tokens) and is already synced to OneDrive | `gs_local.bundle` 152 MB, head == current HEAD; `_claude_sync.tar.gz` 150 MB / 2,697 entries | Scan for credentials, rotate, delete; ignore `*.bundle`, `*.tar.gz`, `_to_delete/` now | hours |
| REPO-6 | high | verified | Two deployable roots; the stale root `vercel.json` has **no headers key at all** — deploying it silently strips CSP, HSTS, and clickjacking protection | root `vercel.json` vs `greenstreet_frontend/vercel.json`; provenance: `24c0323` 2026-08-06 vs `19b4ac4` 2026-06-25 | Delete/archive the root deploy config; document the canonical path | hours |
| REPO-7 | high | likely | CI exists but has never run and cannot pass — the workflow is untracked, and its guard reads the gitignored `api/index.js` | `git status` → `?? .github/`; ci.yml step `fs.readFileSync("api/index.js")` | Fix the ignore, commit `.github/`, confirm a green run, add branch protection | minutes |
| REPO-8 | high | verified | Commit discipline cannot reconstruct what was live on a date — HEAD's message is literally `commit`, no tags, no releases | `git log --oneline -20`; `75b138e commit` | Conventional commits + commit-msg hook + tag every deploy | hours |
| REPO-9 | medium | verified | React/Firebase/Tailwind sit in devDependencies while `puppeteer` (~300 MB Chromium) sits in production | `greenstreet_frontend/package.json`; `firebase.json` predeploy runs `npm run build` | Reclassify; verify with clean `npm ci --omit=dev && npm run build` | hours |
| REPO-10 | medium | verified | .gitignore is wrong in both directions: excludes the human-written backend, tracks the 220 KB machine-generated `_app.cjs` and 1,040 graphify files | `.gitignore:25` `api/` + `*.cjs`; `git ls-files graphify-out \| wc -l` = 1,040 | Invert: ignore generated, track authored | minutes |
| REPO-11 | medium | likely | 2.2 GB `.git` + `node_modules` inside consumer OneDrive sync; corruption is unrecoverable because origin is 47 commits divergent | repo root `C:\Users\serge\OneDrive\Documents\DSCR_LOAN OFFICE`; `du -sh .git` = 2.2G | Move to `C:\dev\`; exclude `.git`/`node_modules` from sync | hours |
| REPO-12 | low | verified | Abandoned scaffolds committed as structure: `DSCR_SOVEREIGN_OS/` (1 file), `perfectproperty/` (empty, already in one .gitignore), `_regen_tmp/`; 4 stale branches all at identical divergence | `firebase.json` ignore array grown to 40+ entries routing around them | `git rm -r --cached`; delete stale branches | minutes |

### 3.4 Docs sprawl & institutional memory decay (9 findings — 2 critical, 5 high, 2 medium)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| DOCS-1 | critical | verified | Inverted tracking: all 15 committed root markdowns are the false-celebration set; every honest audit is untracked and local-only | `git ls-files *.md` → `AUDIT_COMPLETE.md`, `FINAL_SUMMARY.md`…; `git status` → `?? ULTIMATE_AUDIT_2026-07-23.md`, `?? docs/audit-2026-07-23/01..09`, `?? DEPLOY.md` | Commit the honest set, `git rm DEPLOY_NOW.md`, archive the 15 | hours |
| DOCS-2 | critical | verified | Destructive force-push instruction for a leak that never happened (see Top-10 #9) | `MANUAL_ACTIONS_REQUIRED.md:52-85`; `git log --all --full-history -- .env` empty | Delete section 2 | minutes |
| DOCS-3 | high | verified | Owner's own action list 23 days stale: 3 of 6 items not done, 1 done but still listed, 1 rationale now obsolete — and the `/leads` rule flip that killed the lead path is recorded nowhere | `.env.example:28` blank token; `grep AppCheck` = 0; `firestore.indexes.json` = 44 bytes empty; `v11Runner.test.ts` exists (4,194 B) | Convert to a dated register; answer in writing where leads go now | hours |
| DOCS-4 | high | verified | Four contradictory deploy docs; one has said "⏳ Deploying…" for 23 days; the correct one is untracked | `DEPLOYMENT_STATUS.md:1-5`, `DEPLOYMENT_SUMMARY.md:3` (357/357), `DEPLOY_NOW.md` (372/380), `FINAL_SUMMARY.md:5`, untracked `DEPLOY.md` | Delete three, commit DEPLOY.md, ban status docs at root | hours |
| DOCS-5 | high | verified | Three health scores and three test counts published on the same day; three weeks earlier the verdict was "prototype-grade, not production-grade" | `COMPREHENSIVE_AUDIT_REPORT.md:12` 72/100; `AUDIT_SUMMARY_2026-07-15.md:8` 82→88; `SESSION_COMPLETE:5` A- 88; `FULL_STACK_AUDIT.md:5` "3/18 pipeline components are production-ready" | One dated scoreboard backed by an actual test run | hours |
| DOCS-6 | high | verified | 75% of the repo is duplicate copies, poisoning every grep a contractor or agent runs (8,910 of 10,543 tracked .md are in worktrees; 207 files named README.md) | `git ls-files \| grep -c '.claude/worktrees/'` = 15,011 | Same as REPO-2 | hours |
| DOCS-7 | high | verified | ULTIMATE_AUDIT's own roadmap unclosed 15 days later; the one item that looks done (CI) is untracked so it has never run | P1 #10, P2 #11, #13 all open; `QA_FIXPLAN.md` still 2 unchecked / 0 checked boxes | Push `.github/`, confirm a run, close items 11 and 13 | hours |
| DOCS-8 | medium | verified | Persistent memory asserts artifacts that don't exist (`docs/audits-archive/`, `graphify-out/wiki/`), so the AI workflow is briefed from fiction | `ls docs/audits-archive` → absent; `graphify-out/wiki` → absent; memory bulk-dated 2026-06-22 → 2026-07-02 | Correct both claims today; record the 07-23 and 07-27 audits as current truth | hours |
| DOCS-9 | medium | verified | No supersession convention: six `*SUMMARY*/*COMPLETE*/*FINAL*` docs describe one day; only 3 of ~33 carry a date; titles escalate instead of versioning | `AUDIT_COMPLETE.md:5` advertises volume: "Files Created: 15+ / Reports Generated: 5 major reports" | One STATUS.md, overwritten; archive with SUPERSEDED-BY banners; ban FINAL/COMPLETE/ULTIMATE in filenames | hours |

### 3.5 Memory store (15 findings — 3 critical, 5 high, 5 medium, 2 low)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| MEM-1 | critical | verified | The entire Reference tier describes a repository that no longer exists — the provenance chain for all public copy is broken | `RESEARCH/`, `00_MOCs/`, `ANALYSIS/`, `00_website/`, `00_engine/`, `_obsidian_vault/`, `DSCR_Datasets/` all "No such file or directory"; `reference_website_copy_source.md` names `00_website/FRONTEND_HUB.md` as source of truth | Find the tree first — this may be a data-loss incident, not a memory bug | hours |
| MEM-2 | critical | verified | `project_audits_resolved.md` tells every session "audits are resolved, don't re-action" while three newer unread audit cycles sit on disk; asserts API keys/auth/rate-limiting/Firestore all clear as of 2026-06-28 | `docs/audits-archive` absent (commit `ba28ed1` is real); `docs/` holds RELEASE_AUDIT_2026-07-25, audit-2026-07-23, audit-2026-07-27 | Rewrite to a dated, non-expiring-free statement; delete the per-control all-clear entirely | hours |
| MEM-3 | critical | verified | A deferral memory guarded 387 SHA-verified-unique research files ("do not delete anything unique"); the guarded tree and both sides of every mirror pair are gone, and memory never noticed | `_obsidian_vault/` absent; named merge plan under `00_MOCs/_archive/` unreachable | Treat as an incident; search other drives/OneDrive versions before touching anything | hours |
| MEM-4 | high | verified | Newest memory (2026-07-02) claims "Zero old hexes remain" with a commit cite — false on the page it cross-references | `StateLawsPage.tsx:32` `2: "#f97316", 3: "#ff6b6b"`, `:182`; `index.html` 3× `ff6b6b` | Record the rule + known exceptions; ban "zero X remain" phrasing | minutes |
| MEM-5 | high | verified | The largest, most cross-linked memory lists 4 non-existent key files and describes a lender page that no longer works that way; lists as pending two items already done | `PageShell.tsx`, `MarketingSite.tsx`, `wf.tsx`, `marketing-page.html` all absent; `grep Insula\|UWM` = 0; `narrate.ts:19-26` explicitly removed the z.ai proxy the memory names | Split into durable invariants vs a dated repo doc git can age | hours |
| MEM-6 | high | verified | Three incompatible accounts of the business model in memory; 91 lines naming competitor lenders still ship in `src/` despite an "all scrubbed" claim | `src/engine/lenders.ts:99` "LENDER 1: GRIFFIN FUNDING", `:114` FICO provenance; only importer is a test — currently dead code, one `import` from customer-visible | Make `reference_website_copy_source.md` the sole positioning authority; correct "all scrubbed"; decide if lenders.ts should exist | hours |
| MEM-7 | high | verified | A transient plugin bug was recorded as a permanent capability downgrade ("native Read/Edit/Write are unusable; use Desktop Commander MCP") — falsified this session | Native `Read` succeeded twice; only PreToolUse hook matches `Glob\|Grep`; plugin advanced 13.8.1 → 13.11.0 | Delete or rewrite as a one-line diagnostic with a verification command | minutes |
| MEM-8 | high | verified | A one-session "leave it for now" about ~89 fabricated logos/testimonials was hardened into standing policy — **memory is the reason LEG-1 survived 40 days** | `project_home_fabricated_logos.md` "It's a real credibility/compliance issue" + "Do NOT remove or relabel these unprompted"; repeated in MEMORY.md index | Add expiry + trigger ("MUST be raised before launch/MoU/diligence"); move launch blockers to a tracked checklist | minutes |
| MEM-9 | medium | verified | The mandatory first-read for architecture is 6 weeks stale over the deleted tree, and its recorded build stats are off ~10× | CLAUDE.md rule; `GRAPH_REPORT.md` header 2026-06-24, 367 nodes; `project_missing_graphify.md` claims 3,778 nodes | Rebuild or delete; drop the never-true wiki clause; put the build date in the rule | minutes |
| MEM-10 | medium | verified | CLAUDE.md and memory contradict on the rebuild command, and CLAUDE.md — which wins — is the broken one (`python3` absent since at least 2026-06-24) | `which python3` → none; `which python` → /c/Python314/python | Fix CLAUDE.md; memory is not a patch layer for configuration | minutes |
| MEM-11 | medium | verified | Store stopped being maintained 2026-07-02; 5 weeks and 3 audit cycles unrecorded; no per-claim "last verified" dates | Newest file mtime 2026-07-02 17:07; tree since gained `src/marketing/`, `src/routes/`, `src/engine/brain/`, `.github/` | Stamp last-verified dates; treat >30 days as unverified-by-default | hours |
| MEM-12 | medium | verified | Material business terms live only in an untracked chat-memory file: capital-partner confidentiality, the 7-program Cake mapping, banned stats, the placeholder phone, and a third party's public Supabase URL as the program-terms refresh source | `reference_website_copy_source.md`; verified placeholder still live at `MarketingHome.tsx:97` | Move to version-controlled internal doc with an as-of date; memory points at it | hours |
| MEM-13 | medium | verified | Cross-memory contradiction: the design memory sends sessions to `DcNav`, which the navbar memory says was deleted — one step from recreating the second navbar | `grep DcNav\|DcFooter` = 0 hits; only `SiteShell.tsx:6` mentions PageShell in a comment | Update to SiteNav/SiteFooter; add a supersession convention | minutes |
| MEM-14 | low | verified | Structural rot: a dangling wikilink used in two places (frontmatter `name:` uses hyphens, filename uses underscores); 8 of 21 memories are link-orphans — including the three highest-risk ones | `[[project-greenstreet-frontend]]` vs `project_greenstreet_frontend.md` | Normalize frontmatter; add a link-check script | minutes |
| MEM-15 | low | verified | Time-bound items are write-only: a paused engine build (blocked on a spec never delivered), a Q1-2027 re-evaluation, and a Federal-Register-dependent ship date all sit inert with no surfacing trigger | `project_engine_build_constitution.md` "Build is PAUSED pending the real spec"; `project_decisions_v11.md` D2 | Route dated items to a calendar or issue tracker; memory holds reasoning, not reminders | minutes |

### 3.6 Knowledge graph / graphify (8 findings — 2 critical, 2 high, 4 medium)

| ID | Sev | Conf | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|---|
| GRAPH-1 | critical | verified | The mandated graph indexes a stale **duplicate** codebase; 60 live engine modules are invisible incl. `firpta.ts`, `covenantCheck.ts`, `rentIntegrity.ts`, `creditMemo.ts`, `failClosed.test.ts`; it still contains nodes for deleted files | All 76 files resolve to root `src/`; 0 nodes from `greenstreet_frontend`; root-only `diff-check.ps1`, `generate_flux.py` are nodes; `wf.tsx` and `PageShell.tsx` present though deleted | Point graphify at the real root, or delete the CLAUDE.md rule — no graph is safer than a wrong graph | hours |
| GRAPH-2 | critical | verified | The publish step discards 96% of what was extracted: cache holds 8,043 nodes / 13,012 edges / 608 files / 3,581 import edges incl. 127 frontend files; `graph.json` keeps 367 / 478 / 76 / **1** | Same mtime (2026-08-01 18:20) — one run's output | Diff cache vs graph.json to find the path filter; re-emit from existing cache, no re-extraction | hours |
| GRAPH-3 | high | verified | 477 of 478 edges are intra-file — the graph encodes zero architecture, and the report admits it | `GRAPH_REPORT.md:102` "None detected - all connections are within the same source files"; 0 hyperedges | Fix the import-edge loss, re-run community detection, or retire the tool | hours |
| GRAPH-4 | high | verified | All three CLAUDE.md graphify rules are broken: no `wiki/` has ever existed, `python3` isn't installed, and the graph path doesn't resolve from the working directory (and a successful rebuild would write to a gitignored dir) | `find -name wiki` → nothing; `python3` not found; `greenstreet_frontend/.gitignore` lines 6 **and** 74 both ignore `graphify-out/` | Rewrite the block — but only after the graph points at the right tree | minutes |
| GRAPH-5 | medium | verified | The report's own "is this worth it" gate is computed from a corpus size inflated 14.6× | `GRAPH_REPORT.md:3-5` "~2,929,511 words"; actual sum over those 76 files = 201,069 | Treat all self-reported metrics as unverified; re-decide keep/kill at the real size | minutes |
| GRAPH-6 | medium | verified | 75 unnamed "Community N" sections over 367 nodes — 16 singletons, 56% ≤3 nodes — consuming ~78% of a 471-line file that every architecture question must read past | Size histogram; cohesion 0.14 / 0.22; `Token cost: 0 input · 0 output` (no naming pass ever ran) | Enable naming + raise resolution, or strip communities and keep only the god-node list | hours |
| GRAPH-7 | medium | verified | 8.3 MB / 1,040 content-hash cache files committed at root while the same artifact is gitignored twice in the frontend; every rebuild appends, never replaces | `git ls-files graphify-out/` = 1,040 | Ignore `graphify-out/cache/`; `git rm -r --cached` | minutes |
| GRAPH-8 | medium | likely | 44 days stale by its own header, never rebuilt, and code-only — the `engine/brain/` subsystem, 24 audit markdowns, 22 memory files and the research corpus are all outside it | `GRAPH_REPORT.md:1` (2026-06-24); `engine/brain/certaintyGovernor.ts` dated 2026-08-07 absent | Decide what it's for; rebuild on a git hook, not an agent-executed line that has never fired | hours |

---

## 8. Appendix B — Full findings, business & platform lanes (98)

### Content truthfulness — 14 findings (3 critical, 7 high, 4 medium)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| CON-1 | critical | Eight fabricated executives — including a fake "Head of Compliance" and "Compliance Counsel" — are published as real staff and used as blog bylines | src/pages/AboutPage.tsx:7-14 defines TEAM as literal data: `{ id: "team-dave", name: "Dave Feldman", role: "Cofounder & CEO" }`, plus Priya Rao (Cofounder & Head of Quant), Marcus Chen (Head of Lending), Sara López (Head of Compliance), Tobi Okafor (Staff Engineer), Anita Mehta (Product Designer), Jordan Brooks (Account Executive), Hannah Park (Compliance Counsel). Rendered under the heading "The people behind the en | Delete the TEAM array and the team section from AboutPage.tsx. Re-byline every blog post to "Greenstreet Editorial" (already an existing, honest byline used 8 times) or to the real owner's name. Do no | hours |
| CON-2 | critical | 27 fabricated "trusted by" client logos, several using the names of real financial firms (Vanguard, Sequoia, Cornerstone, Lone Star) | greenstreet_frontend/index.html line 979 renders a `testimonial_logo_list` marquee of 27 distinct files under /img/logos/: trust-01-apex-capital-group.png, trust-03-pacific-wealth-advisors.png, trust-11-cornerstone-advisors.png, trust-13-lone-star-capital-partners.png, trust-14-vanguard-asset-management.png, trust-21-sequoia-capital-partners.png, trust-24-liberty-capital-partners.png … through trust-27-capital-bridge | Delete the entire testimonial_logo_list block from both index.html copies and remove /img/logos/trust-*. If social proof is needed before real clients exist, replace with a factual statement of capabi | hours |
| CON-3 | critical | Contact reality is fictional: reserved 555 phone number in the hero CTA, no physical address, no NMLS ID, and three mutually inconsistent email domains | src/marketing/MarketingHome.tsx:97-98 sets the hero CTA to `"Learn More Now<br/>+1 (555) 010-0000"` with `href="tel:+15550100000"`; same value in src/marketing/home-markup.html:951 and vite.config.ts:86. 555-01xx is the NANP range permanently reserved for fiction. Email domains found in real source: `mailto:careers@greenstreetfinance.com` (24 refs, CareersPage.tsx:178) and `mailto:legal@greenstreetfinance.com` (8 ref | Before any traffic: provision one real phone line and one real email on greenstreet.finance, replace all 555 occurrences (grep for `5550100000` and `(555) 010-0000` across index.html, MarketingHome.ts | days |
| CON-4 | high | The site publishes three mutually contradictory user counts on the same domain | greenstreet_frontend/index.html contains three `stat-nb` tiles with the identical label "Greenstreet users" and different values: `70+`, `100+`, and `25+`. Extracted verbatim: `stat-nb u-text-style-h2">70+</div><div class="cs-stat-hero-p u-text-style-large">Greenstreet users`, and the same pattern with 100+ and 25+. | Delete all three. If a real user count exists, publish one number with an as-of date; if it does not, remove the stat tiles entirely rather than picking one of the three. | minutes |
| CON-5 | high | Seven more unsubstantiated performance statistics presented as achieved outcomes, several labeled "Est." but rendered as headline proof | index.html stat tiles: "24 hours / Est. weekly time saved on manual underwriting"; "20% / MoM headcount growth enabled"; "60%+ / Est. Reduction in Loan Origination Cycle Time"; "3 / Legacy Spreadsheets Replaced"; "2 / Legacy Platforms Replaced". src/pages/CaseStudiesPage.tsx:224-228 defines an AGG scoreboard — with the source comment "Real proof, not a decorative diagram" — rendering "$14,800 Hard costs killed at the | Remove every numeric performance claim that has no underlying measurement, and remove the three unattributed testimonial quotes. Keep the capability statements ("50-state rules", "11 lenders") only wh | hours |
| CON-6 | high | Case-study headlines assert real closings that the body copy of the same card explicitly disclaims | src/pages/CaseStudiesPage.tsx:152 headline: "Three appraisals they never paid for. $14,800 in hard costs saved at the desk." Line 163 result: "Three deals that would have failed post-appraisal were killed pre-appraisal, saving $14,800 in hard costs. A non-US investor ITIN file that previously took a week to get a straight answer was approved on the Global program in under three minutes." Yet the same record's metrics | Rewrite the four headlines and the `result` strings to match the hedged metric labels — e.g. "Modeled: three marginal scenarios caught before appraisal spend" and "Modeled: a consolidated 40-door view | hours |
| CON-7 | high | The source template's brand "InvestGO" is still the site's accessible logo name and is announced as a Greenstreet product launch | greenstreet_frontend/index.html: the navigation wordmark is `<div class="nav-logo-go w-embed"><svg aria-label="InvestGO" …>` and the dropdown logo repeats it (`nav_dropdown_text_logo … aria-label="InvestGO"`). The top announcement bar reads: "Greenstreet Finance announces <strong>InvestGO</strong> — the unified DSCR loan platform" linking to /blog/greenstreet-go-launch. 23 "InvestGO" occurrences across the main tree: | Replace both `aria-label="InvestGO"` values with "Greenstreet Finance" and sweep the remaining 21 references. Either rename the announced product to a real Greenstreet name or remove the announcement  | hours |
| CON-8 | high | Advertised DSCR rate tiers are sourced to a "June 2026 rate sheet pull" and are now six-plus weeks stale on a live pricing widget | index.html: `<p>Best available rate tier for 740+ FICO, ≤75% LTV files — June 2026 rate sheet pull` sits directly under the headline stat `6.125%`. The interactive widget hard-codes four priced tiers: `data-rate="6.25% - 6.75%" data-tier="Best tier" data-program="740+ FICO, up to 75% LTV"`, plus 6.75%-7.25% (700-739), 7.10%-7.75% (660-699), 7.50%-8.25% (620-659). A blog headline in MarketingHome.tsx:93 reinforces it: | Either wire the widget to a dated, refreshable source with a visible "as of" stamp and an automatic staleness banner after 30 days, or convert the tiers to non-numeric ranges ("best tier pricing for 7 | days |
| CON-9 | high | Every fabricated social-proof asset and the OpenGraph share image are 404 — /img does not exist in public/ or dist/ | `ls public/` returns only gs-state-map.js, hyperframes, robots.txt, sitemap.xml, step-scroll.js — no img/. `ls dist/` likewise has no img/. Yet index.html references 89 /img/logos/*.png, five /img/generated/av1-5.png testimonial avatars, /img/generated/scenes/*.png, and — critically — index.html:37 `<meta content="https://www.greenstreet.finance/img/generated/hero.png" property="og:image"/>` and :42 the matching twit | Confirm where /img is meant to come from. Regardless: delete the logo and avatar references (per the fabrication findings above) rather than restoring them, and commit one real, honest hero.png so the | hours |
| CON-10 | high | The site markets an active lending team while its own footer and Legal page admit it cannot identify its legal entity, licenses, or role | src/design/SiteShell.tsx:405, on every page: "This site does not currently publish a legal entity name, NMLS identifier, state-license list, or verified lending-partner disclosure. Confirm the responsible licensed party before relying on a financing offer." src/pages/LegalPage.tsx:62: "This website does not currently publish enough verified business-identity, licensing, or counterparty information to establish whethe | Pick one true posture and make the whole site consistent with it. If Greenstreet is a software/analytics provider today, strike "funds", "closed in-house", and "underwritten" from all public copy. If  | days |
| CON-11 | medium | "Updated monthly across all 50 states" is an unverifiable maintenance promise with no update log or timestamp | index.html home stat: `<p>Prepayment rules, usury caps, and business-purpose requirements — updated monthly across all 50 states`, alongside the carousel title "50-state rules, always current". I found no update-log file, no `lastUpdated` field surfaced on the state-rules pages, and the graph/memory trail for this project stops in early July 2026 (memory files dated Jun 22 – Jul 2; graphify-out/GRAPH_REPORT.md dated  | Replace "updated monthly / always current" with a visible per-state "last reviewed: <date>" stamp driven from src/engine/statePppLaws.ts, and add "verify with counsel before relying on this for a tran | hours |
| CON-12 | medium | The Careers page recruits for a Compliance Counsel the About page says is already on staff | src/pages/CareersPage.tsx lists six open roles including `title: "Compliance Counsel", location: "Remote (US)"` (line 52-54), plus Account Executive, Senior Full-Stack Engineer, DSCR Underwriting Lead, Investor Success Lead, Product Designer. src/pages/AboutPage.tsx:14 and :13 and :12 claim those exact roles are filled — Hannah Park (Compliance Counsel), Jordan Brooks (Account Executive), Anita Mehta (Product Designe | Remove the roles that duplicate the (to-be-deleted) team list, or take the Careers page down until hiring is real. Route applications to a mailbox on greenstreet.finance. | hours |
| CON-13 | medium | Five fabricated customer personas with AI-generated avatar images appear as social proof in the homepage hero | index.html visible text yields the names Carlos Martinez, David Chen, Emma Wallace, Layla Kabbani, Maya Reynolds. The adjacent markup is `<div class="hero_user_wrap"><div class="hero_user_visual_wrap is-home"><img alt="" class="hero_user_visual" … src="/img/generated/av5.png"` (also av4, av2). Inference from the /img/generated/ path that the avatars are AI-generated rather than photographs of real users; the names si | Delete the hero_user block and the /img/generated/av*.png references from both index.html copies. | minutes |
| CON-14 | medium | Two divergent site copies plus seven worktree duplicates mean every fabricated claim must be remediated 9 times | The root ../index.html carries the same fabricated set — `grep -c "Dave Feldman\\|Priya Rao\\|InvestGO" ../index.html` returns 5, and it references the same logos/trust-01 … trust-25 files. Additionally `.claude/worktrees/` contains seven full copies of greenstreet_frontend, each with its own index.html, AboutPage.tsx, BlogPage.tsx and ComplianceDashboard.tsx carrying the InvestGO and fabricated-team content (admirin | Decide which index.html is canonical, delete the other, and add a CI grep gate that fails the build on any of: `trust-[0-9]`, `InvestGO`, `555) 010-0000`, or the fabricated staff names. Purge or gitig | hours |

### Strategy vs shipped reality — 15 findings (3 critical, 8 high, 4 medium)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| STR-1 | critical | Site claims to be a direct lender that "funds in-house" while its own footer says it publishes no NMLS ID, no legal entity, and no license list | src/pages/BrokersPage.tsx:145-148 ships the hero copy: "Greenstreet is the broker and the lender — direct to you... Price a deal in under 60 seconds, then we fund it in-house." The global footer rendered on every page, src/design/SiteShell.tsx:405, says: "This site does not currently publish a legal entity name, NMLS identifier, state-license list, or verified lending-partner disclosure. Confirm the responsible licen | Pick one voice and make the whole site consistent with the actual entity. If not licensed: delete every "we fund/we lend/we are the lender" claim sitewide (grep `fund it in-house`, `the broker and the | hours |
| STR-2 | critical | There is no revenue mechanism anywhere in the codebase — the priced product from D6 has zero implementation | Grepping src/, api/, and server.ts for stripe\|checkout\|subscription\|billing\|invoice\|payment returns no payment integration. The three mounted API routes are the entire backend surface (api/_app.cjs:3775-3777): `/api/dscr`, `/api/leads`, `/api/narrate`. The only money-adjacent flow is QualifyModal.tsx:2125 → POST /api/leads → `getAdminFirestore().collection("leads").add(...)` (api/_app.cjs:3441). The comment at Q | Before any further feature work: (1) wire a notification on lead write (email/SMS/Slack) so a submission reaches a human within minutes, (2) put an owner and a response SLA on the `leads` collection,  | days |
| STR-3 | critical | The business pivoted from direct-to-broker to investor-direct without updating the doctrine, and the pivot is half-executed — both audiences ship simultaneously | The mission memory says "$5B+ annually by 2028 via direct-to-broker wholesale channel" and D4 commits to broker pilot outreach with an MoU. But git log for 2026-06-25 is an audience rewrite: "Repurpose /brokers to an investor-direct page", "Repurpose /partners broker portal to the investor workspace", "InvestGO portal: reframe broker-facing labels to investor", "Scrub remaining broker/lender-comparison framing from c | Make the pivot explicit and finish it. Write the decision down (which channel, effective when, what happens to D4/D5/D6), update project_mission_doctrine.md, then either delete /brokers + /partners +  | days |
| STR-4 | high | Engine work proceeded at full speed despite the build constitution declaring the project BLOCKED and instructing "do not proceed" | project_engine_build_constitution.md (2026-06-26): the §6 golden tests and §6.2 canary inputs "were NOT in the message and matches no on-disk doc"; "Canary inputs producing 1.05x/0.88x/−$334 are undocumented anywhere on disk"; user said "do not proceed, wait for next instruction"; build sequence required "write §6 golden tests as FAILING first" then "reproduce §6.2 canary". Since then ~40 engine modules shipped in sr | Either obtain the real §6/§6.2 spec and re-derive the golden vectors against it, or formally retire the constitution and replace it with an honest statement of where the numbers actually come from. Do | days |
| STR-5 | high | The "golden vector" test that underwrites the audit-ready claim is circular and contains a literal tautology | src/engine/golden.test.ts is 37 lines. It imports `verifyGoldenValues` from ./engine and asserts the report it returns. The expected values are defined inside engine.ts itself, so the engine grades its own homework — there is no external oracle. Line 31 is `expect(r.actual).toBe(r.actual); // finite (NaN guard)`, which is unconditionally true and guards nothing (the following line does the real finite check). The tes | Move expected golden values into a separate versioned fixture file (JSON/CSV) with a source and date per vector, derived independently — ideally cross-checked against the Python dscr-core. Delete the  | hours |
| STR-6 | high | Two independent DSCR engines now exist (Python dscr-core and a ~40-module TypeScript engine) with nothing reconciling them; the Slice-5 precursor package has disappeared | project_engine_build_constitution.md names the unresolved fork: "extend Python core vs new TS engine in frontend vs both." It was never resolved on paper; it was resolved in practice by building the TS engine. `ls DSCR_SOVEREIGN_OS/packages` now returns exactly one entry: `dscr-core`. project_slice_status.md records a second package, `packages/dscr-stress` (distributional_dscr.py, 16 tests, 86% coverage, built via a  | Decide the source of truth in writing. If TS is canonical, port the Python golden vectors into the TS fixture and archive dscr-core. If Python is canonical, call it from the API and make the TS engine | days |
| STR-7 | high | Function 04 "Capital Partner Trust" has zero implementation — there is no loan file, so there can be no loan | Grepping all of src/ and api/: `adverse action` = 0 files; `docusign\|e-sign\|1003\|URLA` = 1 file (incidental); `LendingPad\|Encompass\|loan origination system` = 0 files despite D5 formally choosing LendingPad as the v1 LOS on a $26K–$83K 3-year TCO analysis; `credit report\|soft pull` = 3 files (references, not integrations); `SHA-256` hashing = 0. Slices 2 (evidence-vault), 3 (vendor-normalization), 6 (approval-p | Sequence honestly: the next unit of work is intake (application + document upload + a status a human can act on), not another calculator page. Treat LendingPad integration or an interim manual intake  | weeks |
| STR-8 | high | Slice 8 "broker portal" ships as a static page with no authentication and no data — the "two-sided trust platform" has no accounts | src/pages/BrokersPortalPage.tsx is 585 lines and contains no `useState`, no `fetch(`, no Firestore call, no localStorage — grep for all of them returns nothing. src/App.tsx:444: `const handleLoginClick = () => goTo("portal");` — the Login control performs a client-side route change, not authentication. `firebase/auth` is imported by exactly three files: src/firebase.ts, src/test/firebaseStub.ts, and src/components/Co | Either gate the portal behind real Firebase auth with persisted per-user deal state, or remove the Login affordance and stop calling it a portal. Shipping a fake login is worse than no login — it is t | weeks |
| STR-9 | high | Marketing-asset effort exceeds revenue-path effort by roughly an order of magnitude, and the most recent substantive work is video | Measured in the repo root: audit-frames 21M + audit-frames-v3 20M + audit-frames-v4 14M + audit-frames-v5 14M = 69MB of rendered frame sets across four generations; animations/ 9.6M; voiceover/ 2.8M; public/ 95M; 5 mp4 totaling ~19MB (explainer-reel.mp4 12.6MB), all timestamped 2026-08-01; 87 matrix-media-*.png; 13 hf-* HyperFrames composition directories; review-logos-standalone.html at 4MB. Against that, the entire | Freeze media production until a lead reaches a human and a customer can pay. Delete audit-frames v1/v3/v4 (keep v5) to stop the OneDrive sync tax. Re-read D4 before commissioning anything else. | hours |
| STR-10 | high | Nothing here is defensible against a broker with a spreadsheet — every capability is free, ungated, and client-side | All ~25 engine tools ship as public routes with no auth (src/App.tsx lazy-loads 30 pages, none gated), and computation runs in the visitor's browser via src/engineWorker.ts / src/engineService.ts. The lender database is a static file, src/engine/lenders.ts. The doctrine's own invariant 6 — "No invented probabilities (qualitative tiers until funded-loan data)" — concedes there is no funded-loan data. There is no login | Pick one compounding asset and build it: dated/sourced lender guideline records with a reverification cadence (Function 02, and the one the doctrine already specifies), or captured outcomes on real su | weeks |
| STR-11 | high | The site's highest-intent CTA, /book-demo, cannot book anything — and it is the promised fallback when lead submission fails | /book-demo is referenced by 11 CTAs sitewide (second only to /rate-quiz at 16). src/pages/BookDemoPage.tsx:202-208 is the entire action: `const openReview = () => { if (window.openQualify) { window.openQualify(); return; } onNavigate("rate-quiz"); }` — it reopens the same lead modal, or bounces to the quiz. No calendar, no availability, no scheduling integration (grep for calendly returns nothing). Separately, when t | Either connect a real scheduler and a monitored inbox/phone to /book-demo, or change the copy so it does not promise a call and a review team that do not exist. The error-state copy in QualifyModal mu | hours |
| STR-12 | medium | Legal and careers contact addresses are on a different domain than the site — the Terms-of-Service contact likely does not resolve | The site canonicalizes to greenstreet.finance (58 occurrences of https://www.greenstreet.finance across index.html and public/sitemap.xml, set by the 2026-08-06 domain commit). But src/pages/LegalPage.tsx:90 and :505 publish `legal@greenstreetfinance.com`, and src/pages/CareersPage.tsx:178, 264, 267, 471 publish `careers@greenstreetfinance.com` — a different second-level domain that the domain-cleanup commit did not  | Grep for `greenstreetfinance.com` and repoint every address to the canonical domain, then actually send a test message to each and confirm delivery. | minutes |
| STR-13 | medium | A placeholder 555 phone number survives in shipped source and is suppressed only by fragile runtime string replacement | src/marketing/home-markup.html:949 ships `href="tel:+15550100000"` and :951 ships the visible text `Learn More Now<br/>+1 (555) 010-0000`. It is neutralized at runtime by a literal string-pair patch in src/marketing/MarketingHome.tsx:97-98: `["Learn More Now<br/>+1 (555) 010-0000", "Request a<br/>Scenario Review"]` and `['href="tel:+15550100000"', 'href="/book-demo"']`. | Edit home-markup.html directly to remove the placeholder, and keep the test as a regression guard. Do not rely on string replacement to hide content you can delete. | minutes |
| STR-14 | medium | Seven committed agent worktrees and a scraped competitor corpus are duplicated inside the production repo | Hotspot analysis of the last 40 commits shows seven `.claude/worktrees/<name>/` trees (romantic-sinoussi, romantic-lalande, magical-rubin, heuristic-heisenberg, gifted-volhard, festive-nobel, admiring-bohr), each carrying a full duplicate of `RESEARCH/competitor_intel/dscr_capital_partners/pages` (178 files each, ~1,246 total), `00_engine/research/DSCR-Research` (88 files each), `00_MOCs` (59), and in one case a seco | Add `.claude/worktrees/` and `graphify-out/cache/` to .gitignore and `git rm -r --cached` them. Move RESEARCH/competitor_intel out of the shipping repo entirely into a private research store. Then reb | hours |
| STR-15 | medium | Shipped reality is a free, ungated calculator suite with a production studio attached — not a lender, and not yet a lead-gen business | Composite of the above: 38 pages and ~25 engine modules, all free and unauthenticated; one lead form whose output is an unwatched Firestore collection; zero payment, zero intake, zero LOS, zero accounts; the most recent substantive artifacts are five marketing videos dated 2026-08-01 and four generations of rendered audit frames; and the only lender-voice claim on the site is contradicted by the site's own footer. `@ | Adopt lead-gen as the honest interim identity and finish it end to end (notify → owner → SLA → follow-up), which is days of work, not weeks. Defer the lender identity until there is a licensed entity  | days |

### Funnel, UX & revenue path — 11 findings (2 critical, 3 high, 3 medium, 3 low)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| FUN-1 | critical | The qualify form rejects EVERY real submission — the lead funnel has a 100% failure rate | QualifyModal.tsx:2097-2120 builds the payload and always appends two extra keys: `createdAt: new Date().toISOString(),` and `submittedAt: new Date(), // Firestore rules require this field (as Timestamp)`. That object is sent verbatim (line 2130 `body: JSON.stringify(payload)`) to POST /api/leads. The server schema at src/routes/leads.ts:38-67 is declared `.strict()` and its key list contains NEITHER createdAt NOR sub | Delete lines 2117-2118 of src/components/QualifyModal.tsx (`createdAt` and `submittedAt`) — the server already stamps `submittedAt: FieldValue.serverTimestamp()` and `contactConsentAt` itself at leads | minutes |
| FUN-2 | critical | Even when fixed, leads land in a write-only collection nobody is notified about | The only consumer-side reference to the leads data anywhere is the write at src/routes/leads.ts:83 `.collection("leads").add({...})`. Grepping the whole tree for readers returns only the writer, its tests, the rate limiter, and the router mount. firestore.rules:90-92 reads `match /leads/{leadId} { allow read, write: if false; }` — no client can ever read a lead. There is no admin page, no export script, no notificati | Before spending another dollar on traffic: add an immediate notification on successful persist (email or Slack webhook to the deal desk) inside defaultPersistLead, and stand up a minimal authenticated | days |
| FUN-3 | high | The failure escape hatch loops back into the broken form; the only real booking widget lives in the static layer the React app replaces | The submission-failure copy at QualifyModal.tsx:2150 promises a human path: "Try again — or book a call and the review team will take your scenario live." But /book-demo's primary button is BookDemoPage.tsx:312-318 `onClick={openReview}`, and openReview is defined at BookDemoPage.tsx:202-208 as `if (window.openQualify) { window.openQualify(); return; }` — it re-opens the same modal that just failed. Grepping BookDemo | Put a real terminus on /book-demo: embed the existing HubSpot meetings link (or a plain tel:/mailto: with a named person) as a first-class option, not behind openQualify. Change the modal's failure CT | hours |
| FUN-4 | high | Zero conversion events — the business cannot measure a single funnel step | GA4 is installed but inert. index.html:126 contains `gtag('config', 'G-JERVW0S7X4')` plus `google_tags_first_party` and two `gtag('set', 'developer_id...')` calls. Counting every gtag call in the repo: 1x config, 1x js, 2x set, and ZERO `gtag('event', ...)`. There is not one conversion, form_start, form_submit, generate_lead, tool_used, or CTA_click event anywhere in src/. No second analytics tool exists either — pla | Instrument the four events that actually matter before anything else: qualify_open, qualify_step_completed (with step number), generate_lead (on the 202), and lead_submit_failed (on the catch at Quali | hours |
| FUN-5 | high | The home page's styling is loaded from a Webflow CDN the business no longer controls | index.html:126 pulls a render-blocking stylesheet from a third party: `<link ... href="https://cdn.prod.website-files.com/67d0a8a9156b7b7bd46ffdfd/css/greenboard-00.shared.57c976e80.css" integrity="sha384-V8l26AWsq7po2+QnyQbr/k5OR9/wpEXYLAlJX25NoXqMnlYq3WdjjChIJWvBWY3B" rel="stylesheet">`. Counting external hosts referenced in index.html: cdn.prod.website-files.com x9, meetings-na2.hubspot.com x4, cdn.jsdelivr.net x3 | Self-host the Webflow stylesheet and the jsdelivr scripts into public/ and repoint index.html at local paths. This also removes a third-party dependency from the render path, which improves LCP. Keep  | hours |
| FUN-6 | medium | Broker page promises submission the product explicitly refuses to do | Within one file, BrokersPortalPage.tsx:24 disclaims: "The workspace does not submit files for underwriting or represent a provider's decision process." Then BrokersPortalPage.tsx:515 promises: "Price and submit in minutes." Line 145-147 also markets "your pipeline, one login," and line 34 claims "Track every active deal across your pipeline so nothing slips through the cracks" — while line 24 scopes the same feature  | Change "Price and submit in minutes" to language the product actually delivers ("Price and package in minutes"), and align the "pipeline" claims with the scenario-tracking scope stated on line 24. One | minutes |
| FUN-7 | medium | BrokersPortalPage is a marketing landing page, not a portal — verdict on the direct question | The page contains no auth primitives whatsoever: no signIn, useAuth, currentUser, onAuthStateChanged, password field, file upload, or deal-status fetch. Its four CTAs are all router pushes — BrokersPortalPage.tsx:239 and :541 `onClick={() => onNavigate("portal")}`, :273 `onNavigate("rate-quiz")`, :561 `onNavigate("dscr-calculator")`. The file's own comment at line 225 concedes the split: `{/* CTA block — real Firebas | Decide and then say it plainly: either build broker deal submission (multi-week), or reposition this page as the analytics-workspace marketing page it is. Also note the hardcoded `"default-app-id"` pa | weeks |
| FUN-8 | medium | An interruptive auto-open modal fires on every desktop visitor and is guaranteed to fail | QualifyWidget.tsx:29-31 and :140 — `AUTO_OPEN_DELAY_MS = 30_000`, `SCROLL_THRESHOLD = 0.55`, `timerRef.current = setTimeout(trigger, AUTO_OPEN_DELAY_MS)` — the modal opens itself after 30 seconds OR 55% scroll depth, whichever comes first, for every desktop visitor who has not previously seen it. It writes `localStorage.setItem(STORAGE_KEY, "1")` at line 91 with no consent gate. It is deliberately suppressed under 47 | After fixing #1, gate auto-open to marketing pages only (never on /tools/* or the calculator, where it interrupts a task), raise the delay, and A/B it against the sticky pill alone using the new analy | hours |
| FUN-9 | low | Route round-trip mismatch: the /partnerships URL never returns to its own page | App.tsx:293 maps the view to a URL: `case "brokers-partner": return "/partnerships";`. But src/router/resolve.ts:94 maps that URL back to a different view: `"/partnerships": "portal", // old Partnerships page now lands on the INVESTGO dashboard`. So navigating to brokers-partner sets the address bar to /partnerships, and reloading, bookmarking, or sharing that URL lands the visitor on the INVESTGO dashboard instead. | Add a test that asserts `resolve(pathFor(view)) === view` for every view in the union type. That one test catches this class of drift permanently. | minutes |
| FUN-10 | low | Bundle budget is loosened rather than enforced; 512KB Firebase chunk on the critical path | vite.config.ts:107 sets `chunkSizeWarningLimit: 800` — Vite's default is 500, so the config raises the threshold to silence warnings rather than setting a budget. Built output sizes: firebase 512K, index 448K, vendor 256K, react 192K, BlogPage 156K, ComplianceDashboard 124K, gsap 116K (uncompressed, from dist/assets/). manualChunks is configured (vite.config.ts:110), and there is no CI size gate anywhere. | Confirm the firebase chunk is dynamically imported only on authenticated routes, not pulled in by the root App import graph. Add a CI bundle-size assertion on the entry chunk and restore the warning l | hours |
| FUN-11 | low | Alt-text coverage is thin for a site with a real ADA-litigation profile | Across all of src/ there are 193 `aria-*` attributes but only 6 occurrences of `alt=` in .tsx files. The a11y baseline that does exist is real and verified — index.css:86 `.gs-skip-link:focus`, index.css:639-643 `a:focus-visible, button:focus-visible, [tabindex]:focus-visible` plus checkbox/radio focus styles, index.css:1657 a global `:focus-visible`, the modal carries `role="dialog" aria-modal="true" aria-labelledby | Run axe-core or Lighthouse a11y over the top 10 revenue pages and fix what it reports; add `aria-hidden="true"` to decorative SVGs and real alt text to content images. Do not publish an accessibility- | hours |

### Engine credibility & model risk — 10 findings (2 critical, 3 high, 4 medium, 1 low)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| ENG-1 | critical | Per-field provenance is meticulously maintained in the data layer and then entirely discarded at the UI — including fields explicitly flagged "do not present as fact" | lenders.ts (110KB, 19 named real lenders) carries a 4-level provenance label, source string and asOfDate on nearly every datapoint — 238 occurrences of `source:`. Griffin Funding's max loan amount is stamped: `loanAmountMax: dp(4_000_000, 'UNVERIFIED', 'Griffin site: jumbo to $4M in-house. $20M figure is UNVERIFIED — do not present as fact.', '2026-06', ...)` (lenders.ts:131). Kiavi is `sourceType: 'VERIFIED_SECONDAR | Do not add more disclaimers. Make the type system refuse to render a bare value: change LenderProgram consumers so a `LenderDataPoint` cannot be unwrapped to a primitive without also emitting its prov | days |
| ENG-2 | critical | An engine that emits priced offers, lender matches and state-law conclusions has no accountable owner — the site itself states it publishes no NMLS ID, no entity name and no license list | src/design/SiteShell.tsx:405 renders to every visitor: "This site does not currently publish a legal entity name, NMLS identifier, state-license list, or verified lending-partner disclosure. Confirm the responsible licensed party before relying on a financing offer." LegalPage.tsx:78 repeats it. LegalPage.tsx:167 disclaims that output is "not a pre-screen, quote, program match, commitment to lend, rate lock, or credi | Decide which the product is and stop straddling: either (a) a pure educational calculator — then remove named-lender matching and any output that resembles a price, or (b) a broker platform — then for | weeks |
| ENG-3 | high | The staleness control is structurally incapable of firing — it measures against a hardcoded constant, not wall-clock time, and its runtime function is dead code | dataVintage.ts:30 `export const REVIEWED_AS_OF = '2026-08-08';` with the header comment: "Every staleness computation is relative to this constant rather than `Date.now()` so CI results are deterministic." dataVintage.test.ts:98 tests "no dated dataset is more than 2x its refresh cadence behind REVIEWED_AS_OF" — i.e. constant vs constant. The registry's own header concedes "Nothing in this file gates or changes any m | Add a runtime gate, not just a CI note: compute age against `Date.now()` at render, and when any dataset exceeds 2x cadence, escalate from the quiet grey line to a blocking banner and suppress numeric | hours |
| ENG-4 | high | The state prepayment-penalty engine mis-cites Illinois statute and asserts an entity-borrower legal test that maps to no identified authority | statePppLaws.ts:125 — `statutoryReference: '815 ILCS 137/5 (Predatory Lending Database Act) + 815 ILCS 205/4.1'`. 815 ILCS 137 is the Illinois High Risk Home Loan Act; the Predatory Lending Database Act is a different statute at 765 ILCS 77. The parenthetical names the wrong act for the citation given, and 137/5 is the definitions section rather than the operative prepayment provision. The same entry (line 118-123) a | Spot-audit all 48 entries against primary sources and record a verifier name plus a link per state, not just a `lastVerified` month. Until that pass is done, downgrade every entry to a non-citational  | days |
| ENG-5 | high | A legal effective date that determines which statute governs is hardcoded as `= true` rather than derived from the loan's note date | statePppLaws.ts:2405, inside `checkPPPWithBranching()`: `const isMnPostHf3437 = true; // eff. Aug 1, 2026 — we're past that`. It is consumed at line 2424 (`if (st === 'MN' && isMnPostHf3437)`) to return `allowed: true` with the reason "MN HF 3437 (eff. 8/1/26): Business-purpose DSCR loans NOT reached by § 58.137." The branch input carries no note/closing date to test against. The underlying claim itself — "HF 3437 pa | Add a required `noteDate` to the branch input and derive every time-dependent legal branch from it (`noteDate >= '2026-08-01'`). Do the same for the indexed thresholds — reject or warn when the deal y | hours |
| ENG-6 | medium | The material data-staleness disclosure is deliberately styled to be inconspicuous, by documented intent | src/design/DataVintageLine.tsx renders `DATA_VINTAGE_DISCLOSURE` ("Market data as of [date]. Rates, lender terms, and state rules are dated research — verify with current sources before relying on them.") at `fontSize: 12` in `rgba(0,55,56,0.42)` on light grounds / `rgba(238,239,211,0.52)` on dark. The component's own docblock states the intent: "Deliberately unstyled beyond a muted tint: it belongs at the bottom of  | Raise to body-text size and full contrast, place it above the numbers it qualifies rather than beneath them, and escalate to a visually distinct banner when the data is past cadence (pairs with the ru | minutes |
| ENG-7 | medium | A 32KB after-tax engine (bonus depreciation, §1250 recapture, NIIT, passive-loss, after-tax IRR) carries exactly one disclaimer string on one result object | taxEngine.ts is 32,464 bytes covering OBBBA bonus depreciation, IRC §167/§168/§469/§1250/§1245/§1411 and cost segregation. `grep -in "tax profession\|tax advis\|CPA\|not tax advice\|consult"` across the whole file returns a single hit, line 690: `disclaimer: 'Tax figures are estimates dependent on investor federal bracket, MAGI, REP status, filing status, entity type, state tax law, and cost segregation election. Con | Attach the disclaimer to every exported tax result shape rather than one, and make the renderer unable to display a tax figure without it. Rewrite blog examples to name the assumption set inline. | hours |
| ENG-8 | medium | Quoted rates are silently clamped at the bounds — the extreme deals, where accuracy matters most, are exactly where the number is wrong with no flag | engine.ts:705-708: `const rate = BASE_RATE_ANCHOR + totalBps / 100;` then `return Math.min(Math.max(rate, RATE_FLOOR_PCT), RATE_CEILING_PCT);` with `RATE_FLOOR_PCT = 5.0` and `RATE_CEILING_PCT = 12.0` (lines 121-122). The comment explains the intent — "floor 5.0% (sub-SOFR impossible), ceiling 12.0% (usury red-line)" — but the function returns a bare number with no indication the clamp fired. | Return a flag alongside the rate when either bound binds, and have the UI show "outside modelled range — pricing requires manual review" instead of the clamped figure. | minutes |
| ENG-9 | medium | State-law matrix is 7+ months past its own verification date on 13 of 48 states, and the registry honestly records it while the product keeps serving it | dataVintage.ts:129-137 registers `statePppLaws` at `asOf: '2026-01-01'` with SEMIANNUAL cadence, noting "Two cohorts exist: 35 entries at '2026-06' and 13 entries at '2026-01'. This registry records the OLDEST." My own count of `lastVerified` in statePppLaws.ts confirms exactly 35 at 2026-06 and 13 at 2026-01. Today is 2026-08-07. | Re-verify the 13 stale states before any broker-facing launch, and record per-state verifier and source URL. Wire the per-dataset age to a visible per-page badge so a StateLaws visitor sees the age of | days |
| ENG-10 | low | Named third-party lenders' production volumes and internal metrics are republished as fact, mixed with inline [UNVERIFIED] tags that never render | lenders.ts:149, Griffin Funding `notes`: "May 2026 production: 62 loans, $20.79M, avg DSCR 1.14. Avg loan $335K (= $20.79M / 62 loans). Avg FICO 729 [UNVERIFIED]. Closing: 6 days fastest, 34 days avg." The `[UNVERIFIED]` marker is inline prose inside a string, not a structured field, so no renderer can act on it — and the provenanceDetails array at line 164 separately concedes "Avg FICO 729 — provenance UNVERIFIED, s | Strip third-party production statistics from shipped data entirely — they add no underwriting value and carry only downside. If retained, move every qualifier out of prose into structured fields the r | hours |

### Deployment topology & ops — 14 findings (3 critical, 4 high, 6 medium, 1 low)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| INF-1 | critical | The Vercel serverless function entry point is gitignored — a clean clone cannot deploy a working API | `git check-ignore -v greenstreet_frontend/api/index.js` returns: `greenstreet_frontend/.gitignore:25:api/\tgreenstreet_frontend/api/index.js`. Line 25 of that .gitignore is a bare `api/` sitting under the comment `# Build outputs`. `git ls-files greenstreet_frontend/api/` returns exactly one path — `greenstreet_frontend/api/_app.cjs`. `api/index.js` is NOT in git. Yet `greenstreet_frontend/vercel.json` declares `"fun | Delete the `api/` line (and reconsider `*.cjs`) from greenstreet_frontend/.gitignore, then `git add -f greenstreet_frontend/api/index.js` and commit. Verify with `git ls-files greenstreet_frontend/api | minutes |
| INF-2 | critical | CI has never run and cannot run — .github/ is untracked AND is in the wrong directory for GitHub Actions | `git status --porcelain greenstreet_frontend/.github` → `?? greenstreet_frontend/.github/` (untracked). Separately, the file lives at `greenstreet_frontend/.github/workflows/ci.yml` — a subdirectory. GitHub Actions only reads `.github/workflows/` at the repository root, and the repo root is `C:/Users/serge/OneDrive/Documents/DSCR_LOAN OFFICE`. Meanwhile DEPLOY.md asserts the opposite as settled fact: "CI runs a 'Veri | Move the workflow to `C:/Users/serge/OneDrive/Documents/DSCR_LOAN OFFICE/.github/workflows/ci.yml`, add a `defaults.run.working-directory: greenstreet_frontend` (and `cache-dependency-path`), commit i | hours |
| INF-3 | critical | The only correct deployment runbook is untracked, while three stale/contradictory ones are committed | `git status` shows `?? greenstreet_frontend/DEPLOY.md` — untracked. DEPLOY.md is the accurate document ("Production is deployed through the Vercel project greenstreet-frontend... canonical public host is https://www.greenstreet.finance"; "Firebase configuration is retained as a historical/alternative deployment path, not as the production host"). What IS committed: DEPLOYMENT_STATUS.md ("# 🚀 DEPLOYMENT IN PROGRESS .. | Commit DEPLOY.md. Delete DEPLOYMENT_STATUS.md, DEPLOY_NOW.md and DEPLOYMENT_SUMMARY.md from git (they are point-in-time chat logs, not documentation) or move them to docs/archive/ with a header markin | minutes |
| INF-4 | high | Two divergent full copies of the app in one repo; which one is production is decided by a setting that is not in the repo | `diff -q index.html greenstreet_frontend/index.html` → "Files ... differ". `diff -q src/serverApp.ts greenstreet_frontend/src/serverApp.ts` → "Files ... differ". The repo root carries its own complete `src/` (App.tsx, engine/, pages/, routes/, serverApp.ts, function.ts), its own `index.html` (224KB), `vercel.json`, `firebase.json`, `.firebaserc`, and `api/index.ts`. All of these are TRACKED. There is no `.vercel/proj | Pick one tree (greenstreet_frontend, per DEPLOY.md) and delete the root duplicate's src/, index.html, api/, vercel.json, firebase.json in a single commit — after diffing to salvage anything only prese | hours |
| INF-5 | high | Production marketing images are not in git and not produced by the build | `greenstreet_frontend/public/` contains only: gs-state-map.js, hyperframes/, robots.txt, sitemap.xml, step-scroll.js. There is no `public/img/`, and `git ls-files greenstreet_frontend/public/img` returns 0 files. But `src/marketing/home-markup.html:898` references `src="/img/resources/financial_dashboard.png"`, `/img/resources/commercial_building.png`, `/img/resources/residential_townhouse.png`, `/img/resources/offic | Locate the actual /img/resources/* files (likely in the existing dist/img or the repo root), move them into `greenstreet_frontend/public/img/resources/`, commit them, and confirm `npm run build` copie | hours |
| INF-6 | high | Firebase Hosting is a fully wired second production path that can ship an unbuilt, stale dist/ at any moment | Two identical firebase.json files exist (root and greenstreet_frontend), both with `"hosting": {"public": "dist", "rewrites": [{"source": "/api/**", "function": "api"}, {"source": "**", "destination": "/index.html"}]}`. `src/function.ts:5` exports the matching handler: `export const api = onRequest(`. `.firebaserc` pins a real project: `{"projects": {"default": "gen-lang-client-0809198072"}}`. Critically, the `predep | Decide now: if Firebase is not the host, delete both firebase.json hosting blocks (keep the firestore rules/indexes blocks, which are still needed) and remove the root .firebaserc, or rename them to f | hours |
| INF-7 | high | .env.production.example (tracked) points production at a domain the business does not own | `C:/.../DSCR_LOAN OFFICE/.env.production.example`, header: "Copy this to .env for production deployments." Body: `APP_URL=https://greenstreet.com` and `ALLOWED_ORIGINS=https://greenstreet.com,https://www.greenstreet.com`. The actual domain everywhere else is greenstreet.finance — DEPLOY.md ("canonical public host is https://www.greenstreet.finance"), the production config block (`ALLOWED_ORIGINS=https://www.greenstre | Correct both lines to greenstreet.finance, or delete this file entirely — DEPLOY.md's 'Production runtime configuration' block already carries the correct values and the Vercel dashboard is the real s | minutes |
| INF-8 | medium | AI narration cannot work in production: requireAuth gate plus a SPA that never sends a token and never calls the endpoint | `src/serverApp.ts:144`: `app.use("/api/narrate", narrateLimiter, requireAuth, narrateRouter);`. `src/middleware/auth.ts:126-131`: requireAuth 401s whenever `!req.user`, and req.user is only ever attached from an `Authorization: Bearer` header. `grep -rn "getIdToken\\|Authorization" src --include=*.ts --include=*.tsx` returns ZERO matches — the SPA has no code path that obtains or attaches a Firebase ID token. The onl | Decide the feature's status. If shipping: add getIdToken()-based Authorization headers to the client fetch layer and wire an actual caller, then test end-to-end signed-in. If not shipping: remove VITE | hours |
| INF-9 | medium | Rate limits are per-instance and reset on every cold start; the durable store is documented but not enabled in production | src/serverApp.ts defines `leadLimiter = createLimiter("leads", 15 * 60 * 1000, 5)` and `apiLimiter = createLimiter("api", 60 * 1000, 120)`. The .env.example comment is explicit: "The default in-memory rate-limit store resets on every serverless cold start and is not shared between concurrent instances, so configured limits are really per-instance." The durable alternative `RATE_LIMIT_FIRESTORE=true` is commented out  | Set RATE_LIMIT_FIRESTORE=true in Vercel, add a Firestore TTL policy on the `expiresAt` field of the apiRateLimits collection (as .env.example instructs), and add both to DEPLOY.md's production configu | hours |
| INF-10 | medium | Error monitoring is a permanent no-op: @sentry/react is not installed, is not CSP-allowed, and initSentry() is never called | src/monitoring/sentry.ts dynamically imports "@sentry/react" via `new Function("m", "return import(m)")` inside a `try { } catch { }` whose catch body is the comment "Package missing or blocked — silent no-op." @sentry does not appear in package.json dependencies (@anthropic-ai/sdk, cors, dotenv, express, express-rate-limit, firebase-admin, firebase-functions, pino, pino-pretty, puppeteer, zod) or devDependencies. So | Either install @sentry/react, call initSentry() from main.tsx, set VITE_SENTRY_DSN in Vercel, and add the Sentry ingest origin to the CSP connect-src — verified by deliberately throwing and seeing the | hours |
| INF-11 | medium | No uptime checks, no alerting, no rollback plan, no Firestore backup — for a system holding borrower PII | `grep -rniE "uptime\|rollback\|backup\|pagerduty\|alerting\|on-call\|incident" --include=*.md` across greenstreet_frontend returned only hits from the vendored research corpus (DSCR-Research/*.md) and third-party skill files under .claude/ — nothing describing this product's operations. DEPLOY.md's 'Release verification' section is a manual pre-promotion checklist (`npm ci && npm run lint && npm test && npm run build | Three cheap things, in order: (1) a free external uptime check hitting /health every 5 minutes with SMS/email alert — this alone converts silent outages into known ones; (2) two lines in DEPLOY.md doc | days |
| INF-12 | medium | A 219KB generated server bundle is committed and dirty while its own .gitignore forbids it | `git ls-files` confirms `greenstreet_frontend/api/_app.cjs` is TRACKED. `git status` shows ` M greenstreet_frontend/api/_app.cjs` — it has uncommitted drift right now. It is a machine-generated esbuild output: package.json's build script ends with `esbuild src/serverApp.ts --bundle --platform=node --format=cjs --packages=external --outfile=api/_app.cjs`. Meanwhile greenstreet_frontend/.gitignore lines 20-26 read `# B | Fix the ignore rules first (per #1): remove the bare `api/` and `*.cjs` lines, replace with the specific `api/_app.cjs` and `dist/`. Then `git rm --cached greenstreet_frontend/api/_app.cjs` and commit | minutes |
| INF-13 | medium | Deploy uploads ~180MB / 28,241 files, exceeding Vercel's file limit and forcing archive mode | DEPLOYMENT_STATUS.md states verbatim: "Archive Mode: ✅ Enabled (tgz compression) / Reason: 28,241 files exceed Vercel's 15,000 file limit". DEPLOY_NOW.md is titled "MANUAL DEPLOYMENT INSTRUCTIONS" and opens with "All code is ready. Deployment keeps timing out in background. Please run manually", then "Upload your code (~180MB)". .vercelignore does exclude a lot (docs/, *.md, animations/, hf-*/, scripts/, graphify-out | Delete `_to_delete/` (it is untracked; nothing is lost from git). Move puppeteer to devDependencies. Add `.claude/`, `_to_delete/`, and `voiceover/` to .vercelignore. Then re-measure the upload — the  | hours |
| INF-14 | low | public/.well-known/security.txt is untracked — no vulnerability disclosure channel survives a clean deploy | `git status --porcelain greenstreet_frontend` shows `?? greenstreet_frontend/public/.well-known/`, containing security.txt. `git ls-files public` returns only robots.txt, sitemap.xml, step-scroll.js. Note also `public/gs-state-map.js` is untracked while `src/marketing/stateMap.ts` appears to be the live implementation — a likely orphan. | Commit public/.well-known/security.txt. While there, confirm whether public/gs-state-map.js is still loaded by anything; if not, delete it. | minutes |

### Security & data protection — 9 findings (2 critical, 5 high, 2 medium)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| SEC-1 | critical | Excellent Firestore rules exist in the repo, but nothing deploys them — and the project/database targeting is internally contradictory | `firestore.rules` is well-written, but there is NO deploy path for it. `grep -rn "firestore:rules\|firebase deploy" .github/ package.json scripts/` returns ZERO matches; `.github/workflows/` contains only `ci.yml`. Worse, the targeting is inconsistent: root `.firebaserc` says `{"projects":{"default":"gen-lang-client-0809198072"}}` while `greenstreet_frontend/_to_delete/firebase-applet-config.json` (also in git histor | (1) Run `firebase firestore:databases:list` and `firebase deploy --only firestore:rules --dry-run` against BOTH project ids and confirm which database actually holds `deals`/`leads`/`artifacts`. (2) A | hours |
| SEC-2 | critical | The published Privacy Policy openly states it cannot honor a data-subject request, on a page that collects GLBA nonpublic personal information | `src/pages/LegalPage.tsx:131` — the "Your rights" section literally reads: "A verified privacy-request channel and response-time policy have not yet been supplied for publication." Line 137: "A verified privacy contact channel is pending publication." Line 140: `contactLine: "Privacy contact channel: pending owner verification and publication."` Line 117 (Sharing): "This policy does not identify or verify every downs | Before the form takes one more real submission: publish a GLBA-compliant privacy notice with a named privacy contact (email + mailing address), a stated retention period, an actual deletion workflow,  | days |
| SEC-3 | high | Anonymous auth + no App Check = unauthenticated denial-of-wallet against your Firestore bill | `src/firebase.ts:36` exports `export const loginAnonymously = () => signInAnonymously(auth);`, used by `src/components/ComplianceDashboard.tsx:2`. Anonymous users satisfy `isAuthenticated()` in the rules. The artifacts rule at `firestore.rules:82-84` is `match /artifacts/{appId}/users/{userId}/{document=**} { allow read, write: if isOwner(userId); }` — ownership only, with NO field whitelist, NO size cap, NO document | Enable Firebase App Check (reCAPTCHA Enterprise for web) and enforce it on Firestore — this is the intended control and takes under an hour. Then either disable Anonymous sign-in in the Auth console,  | hours |
| SEC-4 | high | No retention schedule, no deletion path, and no read path for the leads collection — borrower PII accumulates forever with no owner | `src/routes/leads.ts:81-94` writes leads with `submittedAt`, `status: "new"` and no TTL, expiry, or purge field. `firestore.rules:90-92` sets `match /leads/{leadId} { allow read, write: if false; }` — correct for confidentiality, but it means the ONLY way to read a lead is the Admin SDK or the Firebase console. `grep -rn "collection(\"leads\")" src/` returns exactly ONE hit: the write in leads.ts:83. There is no admi | Add a Firestore TTL policy on a new `purgeAfter` field (e.g. 24 months), an authenticated admin export/read route, and a delete-by-email endpoint that satisfies a rights request. Write the retention p | days |
| SEC-5 | high | CSP allows 'unsafe-eval' plus four third-party CDNs to inject script on the page that captures borrower financial data | `greenstreet_frontend/vercel.json`, Content-Security-Policy header: `script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.prod.website-files.com https://cdn.jsdelivr.net https://d3e54v103j8qbb.cloudfront.net https://static.hsappstatic.net`. The same policy also has `form-action 'self' https://meetings-na2.hubspot.com` and `frame-src https://meetings-na2.hubspot.com`. There are no Subresource Integrity constrai | Drop 'unsafe-eval' (Vite production builds do not need it — verify by testing). Self-host the Webflow/jsDelivr assets into `public/` so `script-src 'self'` suffices; if a CDN must stay, pin it with SR | hours |
| SEC-6 | high | A 152MB git bundle containing the entire company's full history sits inside the deployable frontend directory and is not excluded from deploys | `greenstreet_frontend/_to_delete/` contains `gs_local.bundle` (152,407,268 bytes) and `_claude_sync.tar.gz` (150,470,656 bytes), plus `ship2.bundle`, `sync_out.bundle`, `fix_out.bundle`. `git bundle list-heads _to_delete/gs_local.bundle` returns `75b138eadb1495c5e276537a410804dfdc24d57a refs/heads/main` — i.e. the CURRENT HEAD of the entire business repo, full history included. `.vercelignore` excludes `*.tar.gz` and | Move all bundles/tarballs outside the repo tree today, add `_to_delete/` and `*.bundle` to `.gitignore`, `.vercelignore`, and firebase.json's functions ignore list. Then confirm whether any real secre | minutes |
| SEC-7 | high | The entire runtime security configuration exists only in one person's head — no .env on disk, and the only build artifact has empty Firebase credentials | `ls -la greenstreet_frontend/.env` → "No such file or directory". Grepping the built `dist/` for the baked config returns `apiKey:""`, `authDomain:""`, `projectId:""` — the checked-out build was produced with no environment at all. Every security-critical toggle is env-only and undocumented outside `.env.example`: `REQUIRE_AUTH`, `ALLOWED_ORIGINS`, `RATE_LIMIT_FIRESTORE`, `ALLOW_DEV_AUTH_BYPASS`, `FIREBASE_SERVICE_AC | Document every required production variable and its current value-source in a committed `docs/RUNTIME_CONFIG.md`; add a startup assertion that fails loudly in production when `ALLOWED_ORIGINS`, `RATE_ | hours |
| SEC-8 | medium | Two divergent copies of the site and an unreviewed 219KB pre-bundled API artifact mean the audited source may not be the running code | `api/_app.cjs` is 219,711 bytes, modified Aug 7 21:30 — later than every file in `src/` (17:07-20:09) — and `vercel.json` ships it via `"includeFiles": "{api/_app.cjs,dist/engineWorker.cjs}"`. So the deployed API is a committed build artifact, not the TypeScript in `src/`. Separately, `firebase.json` exists identically in both the repo root and `greenstreet_frontend/`, and the root `.firebaserc` points at a project t | Build `api/_app.cjs` in CI from `src/` on every deploy instead of committing it, or add a CI check that rebuilds and diffs it against the committed copy. Delete the duplicate root `firebase.json`/`.fi | hours |
| SEC-9 | medium | Legal page admits no NMLS ID, license list, or business address is published | `src/pages/LegalPage.tsx:78`: "This website does not publish an NMLS ID, a state-license list, or a business address. Do not treat the site as a representation that a product is available in any particular jurisdiction." | Publish the operating entity name, address, NMLS ID if licensed, and the states where the product is actually offered — or gate the intake form to states where you are authorized until that is resolve | days |

### SEO / AI discoverability — 14 findings (2 critical, 3 high, 5 medium, 4 low)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| SEO-1 | critical | Client-only SPA serving 54 sitemap URLs from one identical HTML shell — no SSR, no prerender | server.ts:28-30 dev uses `appType: "spa"`; production is `app.get("*", (_req,res)=>res.sendFile(path.join(distPath,"index.html")))`. vercel.json rewrites `{"source":"/(.*)","destination":"/index.html"}`. firebase.json hosting rewrites `{"source":"**","destination":"/index.html"}`. vite.config.ts has no prerender/SSG plugin (grep for ssr\|prerender returned nothing; only injectSchemaPlugin). Every one of the 54 <loc>  | Prerender to static HTML at build time (vite-plugin-ssg / react-snap / a simple puppeteer crawl over the 54 sitemap URLs) so each route ships its own title, description, canonical, OG tags and JSON-LD | days |
| SEO-2 | critical | OG image is a 404 site-wide, and Open Graph tags are never updated per route | index.html sets `content="https://www.greenstreet.finance/img/generated/hero.png" property="og:image"` and the same URL for twitter:image. `ls public/img` and `ls dist/img` both return "No such file or directory"; `find . -maxdepth 4 -name hero.png` returns nothing; there is no img/ directory anywhere in the repo. Separately, grep for og:/twitter: setters across all of src/ returns exactly two non-test hits, both in  | Create public/img/generated/hero.png (plus per-template OG images for blog/tools), and move og:title/og:description/og:url/og:image generation into the prerender step alongside the canonical. Verify w | hours |
| SEO-3 | high | Built structured data points the brand's logo at greenstreet.com — a domain the business does not own | vite-plugins/inject-schema.ts:12-15 injects `"@type": "FinancialService"` with `"logo": "https://greenstreet.com/img/logo.png"`. `grep -c 'greenstreet\.com' dist/index.html` returns 2 — it is in the shipped build. greenstreet.com is Green Street (Green Street Advisors), an established commercial-real-estate research and analytics firm. | Change the logo URL to the greenstreet.finance origin, add `url`, `sameAs` (LinkedIn, Crunchbase, GMB), `address`, `telephone` and the NMLS identifier to the FinancialService node so the entity is dis | hours |
| SEO-4 | high | Second root-level index.html is a competing homepage whose canonical and og:url both point to https://greenstreet.com/ | C:/Users/serge/OneDrive/Documents/DSCR_LOAN OFFICE/index.html contains `href="https://greenstreet.com/" rel="canonical"` and `content="https://greenstreet.com/" property="og:url"`, 4 total greenstreet.com references, and a different title: `<title>Greenstreet Finance \| The DSCR Engine for Non-QM Brokers</title>` versus the frontend's `DSCR Loans for Rental Property Investors \| Greenstreet Finance`. It sits alongsid | Decide which homepage is canonical, delete or archive the other outside any deployable path, and grep every deployable directory for greenstreet.com before the next deploy. Note firebase.json already  | hours |
| SEO-5 | high | 19 indexable routes with hand-authored SEO metadata are absent from sitemap.xml — including /brokers, the direct-to-broker mission's landing page | Diffing the 54 <loc> entries in public/sitemap.xml against ROUTE_MAP in src/router/resolve.ts and PUBLIC_PAGES in src/seo/routeMetadata.ts. Missing: the 12 canonical /tools/* routes (refi-tracker, arm-reset, monte-carlo, returns, tax-engine, stress-matrix, decision-support, str-underwriting, portfolio, commercial-dscr, construction-bridge, tco-threshold); /brokers (routeMetadata.ts:90 canonicalPath "/brokers"); /stat | Add /brokers, /state-laws, /deal-analyzer, /rate-quiz and the two /legal/* canonicals to the sitemap immediately — these are ungated. Then make the tool reliability hold an explicit, dated decision wi | hours |
| SEO-6 | medium | Sitemap submits two URLs that the pages themselves declare non-canonical | public/sitemap.xml contains `<loc>https://www.greenstreet.finance/privacy-policy</loc>` and `<loc>https://www.greenstreet.finance/terms-of-service</loc>`. src/seo/routeMetadata.ts:177 and :189 set canonicalPath "/legal/privacy-policy" and "/legal/terms-of-service" for those same views. The canonical targets themselves appear nowhere in the sitemap. | Generate sitemap.xml programmatically from PUBLIC_PAGES[].canonicalPath in src/seo/routeMetadata.ts at build time. The route table and the canonical paths already exist in one file; the sitemap should | hours |
| SEO-7 | medium | robots.txt AI policy blocks Google-Extended and CCBot — cutting retrieval, not just training, contrary to its own stated intent | public/robots.txt header comment: "# AI CRAWLER POLICY (Split Strategy) / # Allow retrieval, block training". It then sets `User-agent: Google-Extended / Disallow: /`, `User-agent: CCBot / Disallow: /`, `User-agent: anthropic-ai / Disallow: /`, `User-agent: FacebookBot / Disallow: /`, while allowing GPTBot, ChatGPT-User, ClaudeBot, PerplexityBot. Also `Disallow: /broker-portal` and `Disallow: /dashboard` — neither st | Decide the GEO posture explicitly and write it down: if AI visibility is a growth channel, unblock Google-Extended and CCBot and accept training exposure as the price of retrieval presence. Correct th | minutes |
| SEO-8 | medium | Sitemap has no <lastmod> on any of its 54 URLs | `grep -oE "<lastmod>[^<]*" public/sitemap.xml` returns zero matches. The file instead carries only <priority> (values 0.4–1.0 across 54 URLs) and changefreq — both of which Google has publicly stated it ignores. | Emit lastmod from git file mtime or an explicit publishedAt/updatedAt field on each post when the sitemap is generated at build. Drop priority and changefreq — they are inert bytes. | hours |
| SEO-9 | medium | A second, entirely dead SEO metadata registry — 34 pages of authored titles, descriptions and keywords imported by nothing | src/site/seo.ts (145 lines) exports PAGE_SEO with 34 entries carrying title, description, primaryKeyword and searchIntent (e.g. `primaryKeyword: "DSCR calculator"`, `searchIntent: "Does this property's rent cover the loan payment?"`). `grep -rn "site/seo\\|PAGE_SEO" src/` excluding the file itself returns zero hits — only src/site/seo.test.ts consumes it. Meanwhile src/seo/routeMetadata.ts (431 lines) is the live reg | Merge the primaryKeyword/searchIntent research into routeMetadata.ts (they are genuinely useful and absent there), then delete src/site/seo.ts and its test. One registry, feeding both the runtime head | hours |
| SEO-10 | medium | No entity disambiguation from Green Street Advisors, and every existing identity signal points at the wrong company | Composite: vite-plugins/inject-schema.ts:15 logo on greenstreet.com; root index.html canonical and og:url on https://greenstreet.com/; the FinancialService schema has no sameAs, address, telephone or NMLS field (grep of inject-schema.ts for sameAs\|telephone\|address returned nothing). greenstreet.com belongs to Green Street, a long-established CRE research firm — inference from the domain, not verified by fetch in t | Populate the Organization/FinancialService schema with legal entity name, NMLS ID, address, telephone and sameAs links to owned profiles. Establish those profiles first if they do not exist. Then remo | days |
| SEO-11 | low | Favicons 404 — /img/favicon-32.png and /img/favicon-16.png do not exist | index.html:126 `<link href="/img/favicon-32.png" rel="shortcut icon" type="image/x-icon"/><link href="/img/favicon-16.png" rel="apple-touch-icon"/>`. No img/ directory exists in public/ or dist/ (same root cause as the missing OG image). | Ship public/img/favicon-32.png and favicon-16.png, plus a 180px apple-touch-icon (the current one is pointed at a 16px file, which is the wrong size for that rel). | minutes |
| SEO-12 | low | Two live hosting configurations with SPA catch-alls — duplicate-origin indexing risk, and security.txt will not deploy on Firebase | Both vercel.json and firebase.json exist and both define a catch-all rewrite to /index.html. firebase.json hosting sets `"ignore": ["firebase.json", "**/.*", "**/node_modules/**"]` — the `**/.*` glob excludes dot-directories, so dist/.well-known/ (built from public/.well-known/security.txt, the only file there) would not be published on a Firebase deploy. Whether both origins are actually live was not verified — no H | Pick one host, and on the other either delete the config or serve X-Robots-Tag: noindex for non-production origins. Add an explicit `!.well-known/**` negation or move security.txt out of a dot-path so | hours |
| SEO-13 | low | No hreflang or international targeting despite explicit foreign-investor positioning | `grep -c hreflang` returns 0 in both index.html and src/seo/routeMetadata.ts. Meanwhile src/site/seo.ts targets `"Can Foreign Investors Get a U.S. DSCR Loan? \| Greenstreet Finance"` with primaryKeyword "DSCR loan for foreign investors", there is a /non-us-investors route, and the sitemap includes /blog/dscr-foreign-nationals-itin. | Add at minimum `<link rel="alternate" hreflang="x-default">` and en-US self-references on the internationally-targeted pages. Revisit properly if localized content is ever planned. | hours |
| SEO-14 | low | Entire 34-post blog corpus ships as one 181KB source file to a client-rendered page | `wc -c src/pages/BlogPage.tsx` = 181,362 bytes holding all 34 posts (34 `slug:` entries), with BlogPostPage.tsx at 29,875 bytes acting as template. Combined with the client-only rendering established above. Actual bundle chunking and Core Web Vitals were not measured. | Measure first: run Lighthouse against the deployed site and inspect the built chunk graph. If the blog content is in the main chunk, move article bodies to per-slug lazy-loaded modules or (better) let | days |

### Brand, design system & media — 11 findings (1 critical, 4 high, 4 medium, 2 low)

| ID | Sev | Finding | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| BRD-1 | critical | The shipped homepage is a wall of broken images and a dead hero video — every visual asset 404s | `dist/index.html` references 55 unique `/img/...` paths and two videos (`grep -o "/video/[a-z0-9.-]*" dist/index.html` → `/video/hero.mp4`, `/video/greenstreet-rebuild-v2.mp4`; plus `/img/generated/hero.png`, `av1..av5.png`). But `find dist -name "*.png"` returns ZERO results and `dist/video` does not exist. `greenstreet_frontend/public/` contains only: gs-state-map.js, hyperframes, robots.txt, sitemap.xml, step-scro | Decide first whether the fabricated logos ship at all (they should not). Then move the surviving real assets from `DSCR_LOAN OFFICE/public/{img,video}` into `greenstreet_frontend/public/{img,video}`,  | hours |
| BRD-2 | high | Two different homepages with two different brand promises both ship in the same app | `greenstreet_frontend/index.html:975` H1: "The rent qualifies this loan. Your paycheck stays out of it." with body copy "We price it, structure it, and fund it — one team, no handoffs, no marketplace." Meanwhile `src/marketing/home-markup.html:734` H1: "DSCR financing for real estate investors — qualify on the property's rental income, not personal income tax returns." with "Direct DSCR lending for real estate invest | Pick one canonical homepage source. Either delete the static marketing body from index.html and let index.html be a bare shell, or delete `src/marketing/home-markup.html` and the MarketingHome route.  | days |
| BRD-3 | high | ~124MB of finished marketing media — a complete ad campaign plus narration — has zero distribution | Zero code references, verified by `grep -rl "explainer-reel\\|greenstreet-83s-master\\|greenstreet-61s-landing\\|greenstreet-34s-linkedin\\|greenstreet-16s-preroll" --include=*.html --include=*.tsx --include=*.ts --include=*.js .` (excluding worktrees) → empty. Orphaned inventory: 5 root mp4s = 19.9MB (`explainer-reel.mp4` 12.6MB, `greenstreet-83s-master.mp4` 2.9MB, `greenstreet-61s-landing.mp4` 2.3MB, `greenstreet-3 | Two decisions, not one. (1) Distribution: if the campaign is still on-message, ship it — the 34s LinkedIn cut is the direct-to-broker channel the strategy names. (2) Hygiene: delete audit-frames v1/v3 | days |
| BRD-4 | high | The owner's own banned-color rule is violated on exactly the two pages where color IS the risk message | `src/theme.ts:29-30` states the rule verbatim: "Risk states pull from here; never reintroduce the old bright coral #ff6b6b or orange #f97316." Violations: `src/pages/StateLawsPage.tsx:32` — `const TIER_COLORS: Record<Tier, string> = { 0: dc.emerald, 1: dc.lemon, 2: "#f97316", 3: "#ff6b6b", 4: "#a78bfa", 5: "#9aa3ab" };` (both banned colors, plus an off-system purple and grey). `src/pages/StateLawsPage.tsx:182` — the  | Replace all six call sites with `risk.danger` / `risk.warning` / `risk.dangerOnDark` from theme.ts. Add an ESLint `no-restricted-syntax` rule banning raw hex literals in `src/pages/**` and `src/compon | hours |
| BRD-5 | high | theme.ts declares itself the single source of truth but is documentation, not a constraint — hundreds of hardcoded hexes and a shadow palette | `src/theme.ts:3`: "Single source of truth. Pages must import from here, never redefine colors." Actual census across `src/`: 50× `#eeefd3`, 37× `#003738`, 28× `#d8d958`, 12× `#002a29`, 9× `#006565`, 9× `#004041`, 8× `#7ec8d3`, 7× `#4dbd97` — the tokens are being retyped, not imported. Worse, a shadow palette exists that theme.ts never defines: six near-identical dark teals — `#002a29`, `#00302e`, `#001f20`, `#003a39` | Ship a codemod that replaces every literal matching a theme value with the token import, then add the lint rule from the previous finding. For the six orphan teals, decide whether they are one surface | days |
| BRD-6 | medium | The HyperFrames media library has corrupted provenance — 7 of 13 project folders claim the same identity | Reading `meta.json` in each `hf-*` directory: `hf-deal`, `hf-montecarlo`, `hf-returns`, `hf-step1`, `hf-step2`, `hf-step3`, and `hf-tax` all contain `"id": "hf-statelaws"` — identical to `hf-statelaws/meta.json` itself. Example: `hf-step1/meta.json` is `{"id": "hf-statelaws", "name": "hf-statelaws", "createdAt": "2026-06-25T02:53:26.854Z"}` despite the folder being named hf-step1. Only `hf-dscr`, `hf-lender`, `hf-ste | Rebuild each meta.json id to match its directory, then render each project once and store the output next to its source with a manifest mapping project → output file → the page that consumes it. | hours |
| BRD-7 | medium | The homepage bakes HyperFrames scenes into mp4 — the exact practice the owner banned — while the one compliant implementation sits on a different page | The standing rule is that HyperFrames must run live as a sandboxed iframe playing `window.__timelines.main` on loop, never as `<video>`/mp4. `CaseStudiesPage.tsx` follows it exactly — line 51-54: `<iframe ... sandbox="allow-scripts"`, with `liveSceneDoc()` injecting `t.repeat(-1);t.play(0)` against `window.__timelines.main`, and a poster `<img>` as the reduced-motion fallback. But the homepage does the opposite: `ind | Since the mp4s are 404 anyway, this is the moment to convert: port hf-step1..hf-step5 to the CaseStudiesPage pattern (sandboxed iframe + live timeline + poster fallback) rather than copying the binari | days |
| BRD-8 | medium | 1.3GB of stale agent worktrees inside the app directory hold the only surviving copies of shipped media | `du -sh greenstreet_frontend/.claude/worktrees` → 1.3G across 7 worktrees. `find . -name "hero.mp4"` returns matches almost exclusively under `.claude/worktrees/admiring-bohr-9469c7/`, `festive-nobel-7fbef8/`, `gifted-volhard-f5117b/` etc., each carrying a full `public/video/` copy including `greenstreet-rebuild-v2.mp4`, `hero.mp4`, `step-01-tools.mp4`. | Copy the media out to `greenstreet_frontend/public/` and commit (or push to a CDN) BEFORE deleting any worktree. Then add `.claude/worktrees/` to .gitignore and prune them. | minutes |
| BRD-9 | medium | Infinite floating/pulsing motion survives on NonUsInvestorsPage despite the global no-floating-motion ban | `src/design/dc.tsx:141-142` neuters the sanctioned names — `@keyframes gsFloat{from,to{transform:none;}}` / `@keyframes gsPulse{from,to{opacity:1;}}` with the comment "Floating/pulsing motion neutralized per design taste (no 'flow'/glassmorphism)." But `src/pages/NonUsInvestorsPage.tsx:137-143` declares six page-local infinite animations under different names that escape the ban: `.fn-flow{...animation:fnFlow 1.05s l | Replace the perpetual loops with a one-shot reveal (the `useRevealOnView()` hook already in dc.tsx). Change the enforcement from name-blocklist to a lint rule banning `infinite` in animation shorthand | hours |
| BRD-10 | low | A dead HyperFrames scene ships in the production bundle | `public/hyperframes/how-it-works/step-scene.html` is copied into the build (`dist/hyperframes/how-it-works/step-scene.html` exists), but `grep -rn "step-scene" src index.html public` returns no reference from any consuming file — only self-matches inside step-scene.html. | Wire it into the how-it-works flow or delete it from public/. | minutes |
| BRD-11 | low | CSP frame-src omits 'self', and the only live motion assets on the site are srcdoc iframes | `vercel.json` Content-Security-Policy sets `frame-src https://meetings-na2.hubspot.com` — no `'self'`, no `blob:`, no `data:`. The four case-study HyperFrames scenes render via `CaseStudiesPage.tsx:51-54` as `<iframe sandbox="allow-scripts">` with injected srcDoc. INFERENCE: browsers generally treat `about:srcdoc` frames as inheriting the parent policy rather than gating them on frame-src, so this most likely renders | Add `'self'` (and `blob:` if srcDoc is ever swapped for blob URLs) to frame-src so the intent is explicit, and add a smoke test that asserts the case-study iframe reaches a non-zero rendered height. | minutes |

---

## 9. Appendix C — What the audit itself missed (completeness critic, 12 new findings)

A final agent was tasked with finding what all 14 lanes overlooked. Its results include the corpus-recovery discovery in §0.1.

| ID | Sev | Finding | Evidence | Fix |
|---|---|---|---|---|
| MISS-1 | critical | The About page ships a fabricated eight-person executive team — including a Head of Compliance and a Compliance Counsel — on a lending company run by one person | src/pages/AboutPage.tsx lines 6-15 define `const TEAM = [{ id: "team-dave", name: "Dave Feldman", role: "Cofounder & CEO" }, { name: "Priya Rao", role: "Cofounder & Head of Quant" }, { name: "Marcus Chen", role: "Head of Lending" }, { name: "Sara López", role: "Head of Compliance" }, { name: "Tobi Okafor", role: "Staff Engineer, Engine" }, { name: "Anita Mehta", role: "Product Designer" }, { name: "Jordan Brooks", ro | Before anything else on this list: replace TEAM with the real principal(s) or delete the team section entirely, and delete the fabricated JOBS/roles until there is a real hiring budget. If placeholder |
| MISS-2 | critical | Every published contact channel is non-functional: the legal/security email domain is unregistered (NXDOMAIN), the live domain has no MX, the phone is a reserved fictional number, and the privacy contact is a TODO | DNS resolved live from this machine: `greenstreetfinance.com` returns "DNS name does not exist" for BOTH MX and A — the domain is not registered. That domain is where the company publishes its legal and careers mail: LegalPage.tsx:90 `contactLine: "Questions about these terms? Contact legal@greenstreetfinance.com."`, LegalPage.tsx:505, and public/.well-known/security.txt line 1 `Contact: mailto:legal@greenstreetfinan | Today: register greenstreetfinance.com defensively (or, better, purge it from the codebase and use the domain you actually own), add MX to greenstreet.finance and stand up legal@ / security@ / careers |
| MISS-3 | critical | The only human path in the entire business is one third party's personal HubSpot calendar ("mark-michael"), and leads land in a database with no reader, no alert, and no reply channel | The sole booking destination sitewide, 8 occurrences across src/ and index.html: `https://meetings-na2.hubspot.com/mark-michael/greenstreet-book-a-demo-page`. The slug "mark-michael" appears in exactly four files, all of which are the same embedded link (greenstreet_frontend/index.html, dist/index.html, src/marketing/home-markup.html, root index.html) — the name appears in no memory file, no doc, no About page, and n | Decide and document who owns the lead. Minimum viable: an email/SMS notification on lead write, a real inbox, and a named human with an SLA. Confirm who 'mark-michael' is and whether that HubSpot acco |
| MISS-4 | high | There is no monetization surface anywhere in the product, despite go-to-market monetization research sitting on disk | All 38 routes in src/App.tsx (lines 282-319) enumerated: marketing, portal, dscr-calculator, lender-intel, state-laws, deal-analyzer, borrower-profiles, non-us-investors, str-hosts, vacation-homes, brokers, brokers-partner, investors, faq, blog, blog-post, case-studies, rate-quiz, refi-tracker, arm-reset, monte-carlo, returns, tax-engine, stress-matrix, structure-optimizer, decision-support, str-underwriting, portfol | Pick the model from your own research (per-loan fee as lender vs SaaS seat for brokers vs lead-gen) and build the minimum that proves it: identified accounts, a saved-deal object, and one paid or gate |
| MISS-5 | high | The un-patched copy of the homepage still claims "we underwrite and fund in-house," and the control preventing that claim is an unverified string replacement | Root `C:/Users/serge/OneDrive/Documents/DSCR_LOAN OFFICE/index.html` line 904 contains verbatim: "Direct DSCR lending for real estate investors, foreign nationals, and short-term-rental hosts — we underwrite and fund in-house." plus "Verified pricing, instant program matching". greenstreet_frontend/index.html contains 0 occurrences, because the React app strips it at render: src/marketing/MarketingHome.tsx lines 5-20 | Fix it at the source — edit home-markup.html so the unsupported claim does not exist in the artifact at all, and keep CLAIM_REPLACEMENTS only as a belt-and-braces check that *throws* when an entry fai |
| MISS-6 | high | An LLM is cast as "a DSCR lending advisor" speaking to the borrower and is explicitly instructed not to disclose that it is AI | src/routes/narrate.ts:104 sets the system prompt: `"You are a DSCR lending advisor speaking directly to the real estate investor who will own and fund this deal. Write plain, honest, advisor-to-investor language."` The user prompt at line 99 ends: `"Write 2-3 sentences in plain English directly to the real estate investor who owns this deal. They are NOT a finance expert. Focus on what this means for their deal. Do N | Remove 'Do NOT mention Claude or AI' from the prompt and add a visible, non-dismissible 'AI-generated summary — not advice, verify with a licensed professional' label on every narration. Change the pe |
| MISS-7 | high | An automated qualification engine issues PASS/FAIL decisions on credit applicants with no adverse-action, reason-code, or decision-retention apparatus anywhere in the codebase | Grep across all of greenstreet_frontend/src/ and index.html for `ECOA\|adverse action\|Regulation B\|Reg B\|equal housing\|fair lending` returns zero matches (the only related text is a static heading, LegalPage.tsx:67 '6. Fair-lending notice', whose own body says at line 70 that its presence 'does not establish that Greenstreet Finance is a lender'). The engine nonetheless renders binary outcomes on named borrower s | Decide and write down the regulatory posture (business-purpose only, or not) and have counsel confirm it — the site currently refuses to say what it is. If any pass/fail is ever shown to an identified |
| MISS-8 | high | The research corpus reported as lost is not lost — it is stranded in seven orphaned agent worktrees totalling ~1.3 GB that git does not manage | `ls .claude/worktrees/` under greenstreet_frontend returns seven directories (admiring-bohr-9469c7, festive-nobel-7fbef8, gifted-volhard-f5117b, heuristic-heisenberg-8e3756, magical-rubin-48cfb8, romantic-lalande-7a567d, romantic-sinoussi-77c044), each 187-190 MB, all created 2026-08-01 18:20-18:21. Each contains the tree other auditors declared missing: `00_MOCs, 00_engine, 00_website, 01_research_notes, 99_external | Before any cleanup: copy one worktree's 00_engine/, RESEARCH/, ANALYSIS/, 00_MOCs/, 00_website/ and memory/ out to a permanent, backed-up location and verify against the SHA list in the mirror-diff me |
| MISS-9 | high | No entity, no license, no insurance and no agreements are evidenced anywhere; the footer names a company with no legal form, address, or NMLS | src/design/SiteShell.tsx:403 renders `© 2026 Greenstreet Finance. All rights reserved. Educational ...` — no entity suffix (LLC/Inc), no address, no license line. The root copy uses a different name entirely: index.html:1303 `© 2026 Greenstreet. All rights reserved.` Grep across src/ and index.html for `NMLS\|E&O\|errors and omissions\|state license\|licensed in` yields exactly one hit, LegalPage.tsx:78, which states | Sequence it: form/confirm the entity and publish the legal name, address and state of formation in the footer; get counsel to determine the licensing perimeter for the states you intend to serve (busi |
| MISS-10 | medium | The data assets the business is built on are not on this machine, and the data that does ship is hardcoded constants with no license, source, or refresh path | A full-tree search excluding node_modules for `*.db, *.sqlite, *.sqlite3, *.parquet, *.csv` across C:/Users/serge/OneDrive/Documents/DSCR_LOAN OFFICE returns ZERO files. The memory store's project overview describes '00_engine/data/dscr_engine.db — SQLite DB, 903 MB' and '2.4 GB datasets' as core assets; neither exists in any form on disk, including inside the seven recovered worktrees (00_engine there is 4.6 MB of m | Locate or write off the 903 MB database explicitly, and record the decision. For anything shown to a user, define the source, the license, the refresh cadence and the owner — a two-column table in the |
| MISS-11 | medium | The only backup strategy visible is 291 MB of full-repo archives sitting in a directory named `_to_delete` | greenstreet_frontend/_to_delete/ contains `_claude_sync.tar.gz` 144 MB (Aug 7 01:13), `gs_local.bundle` 146 MB (Aug 7 04:04), plus device_sync.tar.gz 842 KB, device_sync2.tar.gz 402 KB, fix_out.bundle 76 KB, ship2.bundle 52 KB, sync_out.bundle 41 KB — 291 MB total, all created within the last week, in a folder whose name instructs the reader to delete it. These are git bundles and tarballs, i.e. a manual device-to-de | Rename `_to_delete` to `_archive_do_not_delete` today, or verify its contents are fully represented on the remote and then remove it. Push everything of value to the remote, add the untracked audit do |
| MISS-12 | medium | Against real DSCR lenders the product has no application intake, no document upload, no loan status, and no broker portal — the terminal action for every visitor is booking a call | Complete route list (src/App.tsx:282-319, 38 entries) contains no login, account, apply, application, upload, documents, status, pipeline, or admin view. A 'portal' and a 'brokers-partner' route exist, but there is no auth route, no client auth dependency in package.json (firebase-admin is server-side only), and no application object anywhere in src/routes/ (only dscr, leads, narrate, schemas). The single conversion  | Before building more tools, build the thinnest possible end-to-end path: identified account, submitted scenario, a human response with an SLA, and a status the broker can check. One deal completed end |

---

## Confidence & Scope Limits

Aggregated from all six lanes. Nothing below was verified; treat as open.

- **No test suite, build, or CI run was executed** in any lane. All "test exists" claims are source inspection. The three published test counts (357/357, 372/380, 156) were shown to contradict each other, not resolved.
- **No production/live state was observed** — no deployed URL, no Firestore console, no Vercel env vars. Every claim about what production does (including whether `ANTHROPIC_AUTH_TOKEN` is set and whether `/api/narrate` is dead) is inferred from source and config.
- **The missing root directories** (`RESEARCH/`, `00_MOCs/`, `00_website/`, `ANALYSIS/`, `00_engine/`, `_obsidian_vault/`) were confirmed absent at their documented paths only — moved vs. deleted is unresolved, and that distinction decides whether MEM-1/MEM-3 are a data-loss incident or a stale-path problem. **Resolve this first.**
- **`everything_session_complete.zip` (423 MB) and `_claude_sync.tar.gz` (150 MB) were not extracted or scanned** for credentials. Both remain unknowns.
- **The 47 commits on `origin/main` were not inspected**, so the true scope of the split-brain divergence is unmeasured.
- **The 6 engine defects behind the 7 un-held tools were not individually re-verified** — only `DecisionSupportPage`'s hardcoded `TX` was confirmed still present. The burden of proof is on the un-holder, and no evidence of pre-release fixes appears in any document.
- **The four older root audit docs** (`FULL_STACK_AUDIT.md` 43 KB, `QA_REPORT_DEFINITIVE_2026-06-24.md` 44 KB, `ULTRA_REVIEW_2026-06-24.md` 31 KB, `QA_FIXPLAN.md`) were sampled for verdicts only, not read — they may contain still-open items.
- **The two `index.html` copies were never diffed line-by-line**; canonicality rests on commit provenance (`24c0323` 2026-08-06 vs `19b4ac4` 2026-06-25) and mtimes.
- **A second memory system is active and out of scope** — the claude-mem plugin's own store plus an episodic-memory plugin may be injecting independent stale context into every session.
- **Legal characterizations here are risk observations by a non-lawyer auditor**, not legal advice. Items LEG-1, LEG-2, LEG-3 and LEG-6 warrant counsel review; LEG-1 warrants it this week.